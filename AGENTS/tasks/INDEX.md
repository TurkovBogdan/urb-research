# TASKS INDEX

Log of tasks for the project. Each task in a new area — a separate file. Filename format: `YYYY-MM-DD-slug.md`.

## Rules

- Logging a task is unconditional and automatic — never ask the user whether to record it; every user request gets a task entry.
- Create `AGENTS/tasks/YYYY-MM-DD-slug.md` from `AGENTS/tasks/TEMPLATE.md` at the start of a task.
- The slug describes the area, not the action: `auth-flow`, `build-pipeline`, `payment-api`.
- Add an entry to the "In work" table below.
- Fill in "What was done", "Problems", "Result" and set status to `completed` on completion; then move the row to "Completed".

## In work

Tasks currently being worked on.

| File | Date | Description |
|------|------|-------------|
| [2026-08-27-interface-settings-groups.md](2026-08-27-interface-settings-groups.md) | 2026-08-27 | **Completed.** `/settings/interface` пересобрана в анатомию `/settings/modules`: две группы-карточки с пояснением («Интерфейс» — тема, шрифт интерфейса, раскладка реестра, оглавление документа; «Оформление документа» — шрифт текста, кегль, ширина параграфов) на той же сетке `minmax(320px, 440px)`, описание поля отдельной строкой под ним вместо `persistent-hint`, тумблер — `SwitchPanel`, тот же компонент, которым схема рисует bool-поле. Новый `components/settings/SettingsGroup.vue` (заголовок + описание + слот; описание строкой, не markdown — текст свой, а не из схемы бэкенда). Деление по тому, на что настройка действует: во второй группе всё — ручки типографики зоны чтения, поэтому `documentNav` (элемент навигации) остался в первой. Предпросмотр выведен из сетки на полную ширину — он не настройка, а то, на что настройки применяются. Карточки написаны разметкой, а не циклом по дескрипторам: набор полей известен на месте и разнороден, схема нужна там, где поля приходят с бэкенда. `vue-tsc` 0, `web/dist` пересобран, живая проверка на :22040 в изолированном браузере (обе группы, тумблер пишет и снимает `app.nav.document`), консоль чистая. |
| [2026-08-27-mcp-drop-group-icon.md](2026-08-27-mcp-drop-group-icon.md) | 2026-08-27 | **Completed.** С MCP-поверхности групп убрано всё, что не про смысл полки: иконка и позиция `sort`. Аргументы сняты с `group_create`/`group_update`, тул `group_icons` удалён, все group-тулы отдают новый `GroupScan` (code/title/description/updated_at) — ни `icon`, ни `sort` агент не видит ни в одном ответе и не может задать. Поверхности разведены: web-вьюер по-прежнему на `GroupRow` с обоими полями; колонки, CRUD, `icons.py` и пикеры во фронте не тронуты (вид и порядок задаёт человек). `group_list` по-прежнему отдаёт пользовательский порядок — агент его видит, но не выставляет. Абзац про группы в `_INSTRUCTIONS` переписан: вместо «палитра, выбери имя» и `sort` сказано, что вид и место полки — дело пользователя. Тесты: снят снимок палитры и два кейса на имена иконок; порядок и `sort=0` переведены туда, где `sort` живёт (CRUD/web API); добавлены `test_the_agent_never_sees_how_the_shelf_looks_or_where_it_sits` (ни `icon`, ни `sort` в input-схемах `group_*` и в ответах) и обратная сторона — иконка с позицией, поставленные человеком, переживают `group_update` от агента. Тулов **30** (было 31), `uv run pytest --all -q` → **618 passed, 3 skipped**; схемы и инструкции сервера проверены read-only (живой смоук не гонял: ad-hoc скрипт пишет в dev-БД). Попутно поправлен счётчик тулов в `agent-primary.md` (23 → 30, был устаревшим ещё до правки). ⚠ Живой backend отдаёт старую поверхность до рестарта. |
| [2026-08-27-group-color-picker.md](2026-08-27-group-color-picker.md) | 2026-08-27 | **In work (фронт сделан).** Цвет группы исследований: справочник именованных цветов + компонент выбора в дизайн-системе. Палитра — **10 плоских** цветов (9 тонов через 40° по кругу + нейтральный), у каждого три ступени одного тона (OKLCH L 0.80/0.62/0.47, цветность просим 0.20 и подгоняем под sRGB): светлота одна на весь набор, плитка = средняя ступень, обе читаемые ступени промерены против той плашки, на которую ложатся (худшая пара 5.16:1). Набор урезался дважды по слову пользователя (16 градиентных → 12 плоских → 10), и каждый раз тона **переразносились** по кругу, а не выбрасывались: иначе остались бы соседи, различимые только оттенком (amber/yellow, emerald/teal, sky/cyan). Три файла — три роли: `shared/colorTones.ts` (контракт + вары), `features/research/constants/groupColors.ts` (данные, зеркало `groupIcons.ts`), `.color-tones` в `main.scss` (выбор ступени по теме, роли `--gc-ink`/`--gc-fill`/`--gc-swatch`). Компонент `components/ColorPicker.vue` — лоток плоских плиток той же формы, что `IconPicker`, без поиска (весь набор виден разом), выбор = кольцо + галочка, опция «без цвета» отдаёт `null`, волосяная линия внутрь плитки (в светлой теме средняя ступень отходит от лотка лишь на ~2.8:1). **Составной `components/IconColorPicker.vue`** — иконка и цвет одной панелью (на карточке это одна плашка, и оценивают её целиком): в верхней полосе предпросмотр плашки и палитра, ниже набор иконок с поиском; собран из двух примитивов в новом режиме `bare` (снимают свой лоток), две модели `v-model:icon`/`v-model:color`. Цвет доходит до иконок сам: `.color-tones` на корне объявляет роли, а активная плитка `IconPicker` читает `var(--gc-ink, var(--accent))` — отдельно пикер по-прежнему акцентный, вложенный красится выбранным цветом; наследование переменных и есть связь, без пропов и без `:deep()`. Страницы `/design-system/icon-color-picker` и `/design-system/color-picker` на настоящей палитре, включая плашку иконки; под каждым пикером — живое значение модели в кавычках (`picked = 'teal'` / `optional = null`). 🔴 Первый заход резолвил ступень через `light-dark()`, а `color-scheme` объявляет Vuetify на `.v-application` — цвета пошли бы за темой Vuetify, а не за нашей `data-theme`; поймано живым замером, переведено на правило по `data-theme`. Живая проверка в изолированном браузере (обе темы, клики, `aria-pressed`, консоль чистая), `vue-tsc` 0, `web/dist` пересобран. **Второй этап — цвет доехал до группы.** Миграция `rem_006_group` правлена **на месте** (колонка `color VARCHAR(32) NOT NULL DEFAULT ''` после `icon`): исключение из forward-only держится ровно потому, что таблиц групп нигде не было — прод стоял на `rem_005`, а единственная dev-база с `rem_006..008` тем же шагом заменена целиком. **Перенос базы с прода** (`urb-research-stable/runtime/dev/app.sqlite3`): бэкенд гашен **по pid, не `stop-all.sh`** (тот снёс бы соседний stable), старая база отложена в `app.sqlite3.bak-20260827-074521`, копия снята **backup-API** с read-only подключения (источник пишется живым бэкендом), `integrity_check`/`foreign_key_check` чисто; приехало 43 исследования + 20 строк настроек на `rem_005`, сверху `migrate upgrade` (`rem_006..008`), `migrate check` чист. Бэк: `colors.py` — канонические 10 имён зеркалом `icons.py` (имя на записи не валидируется), `GROUP_COLOR_MAX=32`, колонка в модели, CRUD, `GroupRow`, `GroupBody` обеих write-ручек; **на MCP цвет не выставлен** — как полка выглядит, выбирает человек (та же линия, что у `icon`/`sort`). Фронт: модалка группы перешла на `IconColorPicker` (`clearable`), карточка группы и страница группы красят иконку через `groupColorVars(group.color)` + `var(--gc-ink, var(--accent))` — у псевдо-полки «Без группы» остаётся акцент. Тесты **+8** (CRUD пишет/меняет/снимает цвет и режет по ширине колонки, API гоняет его через create/update/list и принимает `""` как «снять», `test_group_colors.py` — снимок палитры + парность с TS-реестром зеркалом `test_group_icons.py`), модуль **255**. Живьём на :22040 на приехавших данных: группа с ракетой в rose рисуется цветом, правка подставляет оба выбора, «без цвета» снимает (в БД `''`, карточка вернулась к акценту); пробная группа удалена — база оставлена как приехала. ⚠ Чужое в дереве: `sources_refetch` переведён на `codes: list[str]`, а `test_search_sources.py` всё ещё зовёт `code=` — 8 падений в полном прогоне, не трогал. |
| [2026-08-27-study-tasks-and-fetch-statuses.md](2026-08-27-study-tasks-and-fetch-statuses.md) | 2026-08-27 | **Completed.** Изучение проекта с упором на систему задач и статусы получения контента. Кода не менял. Планировщик (`core/scheduler`) полностью реализован, но в реестре ровно одна задача — `core/heartbeat`; ни один доменный модуль задач не регистрирует, `web_search` синхронен по замыслу (ни очереди, ни ретраев, reaper только у запросов — `query_expire_stale`, 15 мин). Статусных машин три: `SEARCH_STATUS_*` (прогон) и `FETCH_STATUS_*` (страница) — одинаковая `pending → processing → done \| error`, терминальные; `DOC_*` источника — другая ось (разбор), мостик `_initial_source_status(page)` + `source_document_revive_by_pages`. Живой срез dev-БД: страницы 3119 `error/ConnectError/web_scrapper`, 188 `empty`, 1012 `done`; источники 3857 `error` / 862 `kept` / 579 `filtered` / 24 `pending` (бэкфилл `rem_008` на месте); запросы 1104 `done`, 43 `error`, 23 `pending`, 1 залипший `processing`. Изменилось с момента диагностики: демон `web_scrapper` на 127.0.0.1:19020 **поднят**, страницы от 27.08 качаются (`done`), последний всплеск `ConnectError` — 26.08; исторические ~3.1k мёртвых страниц оживают только явным `sources_refetch` по исследованию. |
| [2026-08-27-researches-list-view-modes.md](2026-08-27-researches-list-view-modes.md) | 2026-08-27 | **Completed.** Раскладка списка исследований — клиентская настройка (`constants/lists.ts` + `lists.researchView` в `stores/settings.ts`, localStorage, **таблица по умолчанию**; починка кривого значения живёт в кодеке, а не у потребителей). Два переключателя на один ключ: полноимённый `VSelect` на `/settings/interface` и `VBtnToggle` (`mandatory`) последним в ряду фильтров реестра — он не сужает список, а перерисовывает его. `ResearchesTable.vue` → **`ResearchesList.vue`** (`mv`): раскладки различаются РОВНО отрисовкой строки, а содержимое, действия, фильтры, постраничность и окна общие → выбор внутри одной карточки, не два компонента. Вынесен `ResearchRowActions.vue` (меню + копирование) — ячейке таблицы и шапке плитки нужен один набор; компонент только спрашивает, окна остаются у списка. Плитки: грид `auto-fill/minmax(300px,1fr)`, подвал прижат к низу (иначе ряд гулял бы от длины описания), счётчики числом над подписью (подписи разной длины, в строку не выстроились бы в столбцы), свои состояния загрузки и пустоты — их давал `VDataTable`. Страница группы следует настройке, но своего переключателя не имеет, как и фильтров. `vue-tsc` 0, `web/dist` пересобран, 611 passed (бэк не тронут), живая проверка на :22040 в изолированном браузере обеих страниц и обоих переключателей, включая стирание ключа при возврате к умолчанию; временные данные удалены, консоль чистая. ⚠ Vitest во фронте нет → переключение запинено живым прогоном. |
| [2026-08-27-researches-list-row-actions.md](2026-08-27-researches-list-row-actions.md) | 2026-08-27 | **Completed.** В меню строки реестра добавлены полка и удаление — в идиоме групповых модалок (`AppDialog`/`ConfirmDialog`, своя копия значений с синхронизацией на открытии, `report: false` → отказ рядом с кнопкой, `busy`+`persistent`). Пункты: положить/переложить (одно окно `ResearchGroupDialog`, различается заголовком; «Сохранить» заперта, пока выбор равен текущей полке), снять с полки (**без окна** — выбирать нечего и обратимо, свой гейт `detaching` от второго клика, отказ отдан тосту), удалить (`ResearchDeleteDialog` поверх `ConfirmDialog` с перечнем потерь из счётчиков строки + красная оговорка). Обе ручки на бэке уже были и не использовались фронтом: `setResearchGroup` лежал мёртвым кодом, `DELETE /researches/{code}` — открытый хвост из задачи про delete-роуты, теперь закрыт на уровне исследования. `afterChange()` перечитывает и список, и полки — у полки счётчик исследований. Тесты **+7** на пути, которыми ходит меню (переезд между полками и сдвиг обоих счётчиков; снятие сохраняет и исследование, и полку; снятие у неразложенного — 200 no-op; отвязанное попадает в фильтр «без полки»; голый код полки резолвится; переезд на несуществующую полку — 404 **и текущая полка на месте**): **612 passed**, `vue-tsc` 0, `web/dist` пересобран. Живьём на :22040 в изолированном браузере пройден весь цикл на одноразовых исследовании и двух полках через MCP (удалены, проверено), консоль чистая. ⚠ Vitest во фронте нет → поведение окон запинено живым прогоном, не тестом. **Правка формулировок:** метафора «полка» отвергнута пользователем — сущность всюду в приложении называется группой; переведено на его же глаголы (привязать / сменить / отвязать), заголовки окон отглагольные существительные под соседние «Изменение/Удаление группы», заодно вычищены две старые протечки в разделе «Группы». ⚠ Остались непереведёнными ~190 русских комментариев/докстрингов по всему модулю и слово *shelf* в MCP-инструкциях для агента — это внутренний словарь, отдельный проход. |
| [2026-08-27-researches-list-match-sources-table.md](2026-08-27-researches-list-match-sources-table.md) | 2026-08-27 | **Completed.** Список исследований приведён к анатомии таблицы источников: фильтры + чипы переехали **внутрь карточки таблицы** под линейку (отдельная `filter-panel mb-3` убрана, отступы 12px / `0 12px 12px` — как у `.doc-filters`), спереди добавлена колонка действий шириной 84 (меню «Открыть карточку» / «Открыть полку» + копирование кода с самогаснущей галочкой, 26px-кнопки и `--v-list-prepend-gap` портированы 1:1 из `DocumentsTable`). Пункт полки гаснет у неразложенного исследования — зеркало «Открыть источник» без url. `ResearchesTable` общий со страницей полки, поэтому фильтры приходят **слотом** `filters`: нет слота — нет ни панели, ни линейки, полка не тронута. Один хелпер `researchesPath` на клик строки и оба пункта меню: исследование и полка делят сегмент маршрута, разводит префикс кода. `vue-tsc` 0, `web/dist` пересобран, 599 passed, живая проверка на :22040 в изолированном браузере (включённый пункт полки — на одноразовых полке+исследовании через MCP, удалены), консоль чистая. ⚠ Спор с памятью `feedback_filters_in_vcard` («фильтры всегда в отдельной `filter-panel mb-3`»): формулировку стоит расширить до «в карточке — своей панели либо карточки таблицы», запись не правил. |
| [2026-08-27-inline-rename-editor.md](2026-08-27-inline-rename-editor.md) | 2026-08-27 | **Completed.** Перенос донорского `InlineEdit` (портал) + переименование на месте в трёх местах: исследование (название = шапка страницы), зона и заметка (название в карточке). Бэк был read-only кроме полок и удалений → добавлены три **узкие** ручки `PUT .../title` (исследование/зона/заметка) по образцу существующей `PUT /researches/{code}/group`: одно поле, остальной текст по-прежнему за MCP. Общий `TitleBody` срезает пробелы **до** проверки длины (имя из пробелов = пустое, 422), потолок = `min()` трёх констант (новая `RESEARCH_TITLE_MAX`). Фронт: `components/InlineEdit.vue` (метрика и высота строки едины во всех состояниях — вход в правку не двигает вёрстку; свои кнопки вместо `VBtn`), обёртка `features/research/components/TitleEditor.vue` (одна на три сущности, `variant`/`heading` наружу; держит только «когда правка закончена» — `submitted` бережёт черновик от фонового обновления), `PageHeader` пробрасывает слот `#title` в `SectionHeader`. Отказ записи гасится в сторе: тост уже показан клиентом, а правка остаётся открытой с набранным текстом; `error` не трогаем — он про отказ чтения раздела. Тесты +13 (`--module=research` 227), полный прогон **599 passed**, `vue-tsc` 0, `web/dist` пересобран, живая проверка на :22040 в изолированном браузере на одноразовом исследовании (создано и удалено через MCP), консоль чистая. 🔴 При живой пробе ручки `curl`'ом затёрто реальное название исследования `RESEARCH@123aba…` (бэкап от 26.08 его не содержит) — поставлено описательное взамен; правило на будущее: проба записи в dev сначала читает старое значение либо бьёт по своей же строке. Найдено и починено вживую: поле открывалось прокрученным в конец длинной строки (`focus()` уводит скролл, `setSelectionRange(0,0)` его не возвращает) → явный `scrollLeft = 0`. Vitest во фронте нет — spec донора не перенесён. |
| [2026-08-27-researches-list-group-filter-and-sort.md](2026-08-27-researches-list-group-filter-and-sort.md) | 2026-08-27 | **Completed.** Список исследований: фильтр по полке + выпадающий список сортировки вместо кнопки «сначала новые», рядом кнопка-иконка направления. Бэк получил белый список `RESEARCH_SORT_COLUMNS` из 7 ключей: три колонки (`created_at`/`updated_at`/`title`) и четыре счётчика таблицы (`area_count`/`query_count`/`document_kept`/`document_filtered`) — счётчики считаются в Python по кодам страницы, поэтому для ORDER BY понадобились **коррелированные подзапросы** (`_children_count`); неизвестный ключ падает в дефолт, как у соседнего web_search. Сортировка по группе не предлагается — она не колонка таблицы, а на странице полки список и так один. Во фронте `groupFilter` разведён с `groupCode`: стор общий со страницей полки, где код — контекст из адреса, а не выбор человека (контекст сильнее, сбрасывается только фильтр). Тесты +6 (даты пишутся напрямую: `precision=0` и одна секунда на все строки), `--module=research` 187, полный прогон **558 passed**, `vue-tsc` 0, `web/dist` пересобран, живая проверка на :22040 в изолированном браузере — все семь ключей, чип полки, консоль чистая. |
| [2026-08-27-web-search-fetch-failures-invisible.md](2026-08-27-web-search-fetch-failures-invisible.md) | 2026-08-27 | **In work (видимость сделана).** Реализовано: `fetch_status`/`fetch_error` в DTO источника (`_source_document_fields` тянет их из join'а страницы) + `_initial_source_status(page)` в `query_search_run` — источник на упавшей странице создаётся как `fetch_error`, а не `pending` (мостик между машинами статусов web_search и research; `DOC_FETCH_ERROR` перестал быть мёртвым кодом, фильтр `sources_list(status="fetch_error")` заработал) + формулировки для агента (фетч ≠ поиск, `fetch_error` терминален — ждать/поллить бессмысленно, `summary` = сниппет движка, оценивать по нему нельзя). Тесты: стаб получил `fetch_raises`, 4 новых кейса; `uv run pytest -q` → **562 passed**, плюс живая проверка через MCP после рестарта бэкенда. Диагностика: почему получение страниц молча падает. Настройка `fetch_engine=web_scrapper`, демон на 127.0.0.1:19020 не запущен → `ConnectError` на каждом батче: **3119** страниц в `error` против 1008 `done`, ошибки идут ежедневно с июля. Наверх ничего не всплывает: `query_search_run` не смотрит на `run.status`/`run.error`; DTO источника не несут ни `page.status`, ни `page.error` (`source_get` отдаёт `body: null` — «пусто» неотличимо от «не скачалось»); статус `fetch_error` объявлен в constants и в CHECK `rem_005`, но **не присваивается нигде** — 0 строк. Как следствие **2406 источников `kept`** и 1375 `filtered` оценены по пустому телу. Плюс отравление дедупа: `_store_results` до-фетчит только `pending`, страница в `error` переиспользуется вечно (3021 мёртвых url не оживут и после подъёма демона), ретраев и reaper'а для страниц нет; доступность fetch-движка не проверяется до сети. Кода не менял. **Второй заход:** повтор получения доехал до интерфейса (кнопка в таблице источников + пункт строки, HTTP-ручки уровня исследования/зоны/одного источника, общий шаг `services/refetch.py`), а MCP-тул `sources_refetch` стал принимать до 6 кодов и отвечать `{sources, skipped}` с причиной по каждому промаху. 636 passed. ⚠ Путь остаётся блокирующим — на сотнях страниц упрётся в 20-секундный таймаут клиента; очередь на планировщике обсуждена и отложена. |
| [2026-08-27-frontend-api-client-errors-routing.md](2026-08-27-frontend-api-client-errors-routing.md) | 2026-08-27 | **In work (разбор).** Порт слоя запросов и показа отказов из portal-mk2 (`api/client/createClient.ts` + `errorText.ts` + `useToasts`/`ToastStack`) + починка маршрутизации и переходов. Донор — за cookie-сессией и CSRF, urb-research без авторизации → переносится не-auth часть: потолок ожидания с раздельным диагнозом timeout/aborted, `redirect: 'manual'`, проверка контракта ответа (`protocol`), `Retry-After`, единая точка «доложить + бросить» + правило `shouldReport`. Baseline фронта: клиент `internal.ts` без таймаута/проверки content-type; показ отказов размазан по 18 `VAlert` и `error.value = e.message` в каждом сторе, тостов нет; экран отказа один (404 маршрута), «сбой»/«нет связи» и `app.config.errorHandler` отсутствуют, `router.onError` только гасит полосу (провал чанка после выкладки = «клик, и ничего»); маршруты без `name`, сайдбар ходит `router.push` вместо `:to` (в DOM нет `<a>`), активный пункт по точному пути → на деталке раздел не подсвечен; `document.title`/фокус/`aria-live` на переходах нет; сплэш в `index.html` жёстко тёмный при светлой теме. Доктрина по формам отказа — `RESEARCH@c1761afd0a83afede5ee0b`, области B и C. |
| [2026-08-27-setup-page-columns.md](2026-08-27-setup-page-columns.md) | 2026-08-27 | **Completed.** `/settings/core`: карточки групп ENV из одной стопки во всю ширину переведены в грид колонками — `repeat(auto-fill, minmax(320px, 440px))` + `align-items: start`. Ширина колонки ограничена 440px (поле шире хуже читается; 460 не давало двум колонкам влезть в ~930px контента), высота карточки — по её содержимому, под 700px одна колонка. Проверено на 1233/1800/640px. Правка только в `SetupView.vue` + `web/dist`. |
| [2026-08-27-research-groups.md](2026-08-27-research-groups.md) | 2026-08-27 | **In work (дизайн).** Группировка исследований: справочник `research_group` (title/description/icon/sort, «больше = выше») + одна nullable FK-колонка `research_index.group_code` (одна группа на исследование, без связующей таблицы). Проработана структура полей; кода нет. Ключевые находки: иконка не может быть свободной строкой (tabler импортируется компонентами, пакет 71 МБ / ~5900 иконок, неизвестная строка рендерится текстом) → нужен curated-реестр имя→компонент; `ON DELETE SET NULL` в sqlite не сработает → отвязка вручную в `group_delete`; `group_code` nullable — осознанное исключение из политики «текст NOT NULL default `''`». Открыто: MCP-поверхность групп, флаг `enabled`, вид «без группы» в UI. |
| [2026-08-27-project-study.md](2026-08-27-project-study.md) | 2026-08-27 | **Completed.** Изучение проекта: записи (CLAUDE.md / MEMORY / tasks / docs / tools / README), дерево `src/` (188 файлов, шесть модулей из `build_modules`), контракт `core/module.py`, MCP-инструкции research (23 тула подтверждены grep'ом), настройки коннекторов и web_search. Прогон `uv run pytest -q` — **446 passed**. Кода не менял. Находки: рабочее дерево несёт 315 незакоммиченных путей от нескольких задач при 5 коммитах истории; `MEMORY.md` 30.1 КБ против лимита 24.4 КБ → грузится частично (жирные строки `core_connectors`/`web_search`); у `research` нет doc-хаба; в «In work» висят строки, помеченные Completed; `stop-all.sh` по-прежнему убьёт соседний stable-инстанс. |
| [2026-08-27-markdown-typography-and-font-choice.md](2026-08-27-markdown-typography-and-font-choice.md) | 2026-08-27 | **Completed.** Исследование по типографике (RESEARCH@2a0d3e1d7e626c8bc4baf0) применено к зоне вывода markdown + выбор шрифта в настройках. **Роли разделены**: `.md-body` = чтение (`--font-reading`, 16px/1.7, `max-width` на running text), `.md-body--compact` = интерфейс (`--font`, 13px, без ограничения ширины — это описания настроек и чат). Отступы заголовков асимметричны (h2 2em/0.75em), первый элемент без верхнего отступа, подряд идущие заголовки схлопываются; `text-box: trim-both cap alphabetic`, `text-wrap: pretty/balance`, курсив снят с цитат, отступы блоков отранжированы по «весу» (абзац→код 1.5em→таблица 2em→hr 3em), пиксели переведены в `em`, иконка пилюли — `1cap`. **Шрифты**: новый `styles/fonts.scss` (@font-face вынесены из main.scss) + Golos Text / Literata / Inter официальными сборками Google по 4 сабсета с `unicode-range` (бинарники не перерезаны → оговорка OFL про RFN не возникает, рядом `OFL-*.txt`); реестр `constants/fonts.ts`. **Настройка** — клиентская: группа `typography` в `stores/settings.ts` пишет `--font`/`--font-reading` **и `--v-font-body`/`--v-font-heading`** на `<html>` (иначе роли Vuetify остаются на Roboto), новая страница `/settings/interface` с двумя селектами (каждая опция набрана своим шрифтом) и живым предпросмотром. 🔴 Две находки при живой проверке: (1) глобальное `p { font-size: 13px }` из `main.scss` **перебило** зону чтения — абзацы остались 13px, наследования не хватает против неслоёного тег-стиля; лечится явным переобъявлением в зоне; (2) `ch` завышает длину строки на кириллице (`ch` = ширина «0», у Onest на четверть шире средней строчной): `70ch` → 87 знаков, `56ch` → 68 — замерено `Range`-подсчётом; по решению пользователя итог **`92ch`** (85–100 знаков). По его же замечаниям во второй итерации: `text-wrap: pretty` → **`balance`** одним правилом на все текстовые блоки; добавлена **настройка размера текста** (14–20px, `--reading-size` на `<html>` → `--prose-size`; вся зона в `em`, поэтому шаг двигает и заголовки, и код, и таблицы, а `--compact` намеренно прибит к 13px); предпросмотр в настройках развёрнут до полного набора конструкций, включая таблицу и блок кода. Третьей итерацией **ширина параграфов** тоже стала настройкой (64/76/92/108/124ch + «Без ограничения», `--reading-measure`; таблицы и код в ограничение не входят). Прогон по реальным телам dev-БД (ComfyUI 44 КБ / 226 строк таблиц, Laravel-тесты 28 КБ / 7 блоков кода, область, заметка) — чисто. Четвёртая итерация — доводка по живым телам: перенос разведён по задачам (`balance` — заголовкам и ячейкам, running text — `pretty`, иначе двухстрочный абзац уходит в две половинные строки); **инлайн-код перестал быть коробкой** — глобальный `code` из `main.scss` протекал бордером (третий случай той же протечки), снят явным `border: none`, фон переведён на плёнку `color-mix(--text 10%)` + `box-decoration-break: clone`; ссылки в тексте подчёркнуты (цвет — единственное различие, пропадающее у дальтоника); уровни ниже h3 разведены цветом и регистром, а не размером (h4 полный / h5 приглушённый / h6 метка капсом); трекинг заголовков как функция кегля; схлопывание соседних заголовков только для нисходящих пар (после h6 крупный h2 больше не прилипает); маркеры списков приглушены; `tabular-nums` в таблицах; линейка цитаты выведена из цвета текста (на `--border` тонула). **Пятая итерация — тема оформления:** режимы системная/светлая/тёмная (`constants/theme.ts`), тёмные токены остались в `:root`, светлые в `:root[data-theme='light']` + `color-scheme`, зеркальная тема `light` у Vuetify, стор пишет `data-theme` и зовёт `vuetify.theme.change()`; «системная» следует за ОС вживую (проверено эмуляцией `prefers-color-scheme` — схема меняется без перезагрузки). Файл `main.scss` оказался почти целиком токен-driven (вне `:root` всего 9 литералов), поэтому светлая схема завелась без переписывания правил. 🔴 Светлая тема вскрыла: (1) блоки кода — shiki на одной палитре `github-dark`, фон `pre` из `--surface` → текст `#E1E4E8` на белом; лечится переобъявлением токенов **на самой панели** (`:root[data-theme='light'] .code-block`), правильное решение (dual-theme shiki) принадлежит задаче markdown-it; (2) `--text-faint` не проходил AA **в обеих** темах — светлая 3.31, **тёмная 3.45** (ровно тот провал, что исследование называло поимённо), подняты до 4.51 / 4.92. Дефолты по требованию пользователя: тёмная / Onest / 14px / 92ch. **Шестая итерация — сплошной аудит контраста:** написан пробник (обход всех текстовых элементов, вычисление фактически закрашенного фона сквозь прозрачные предки, сравнение с порогом AA), прогнан по **14 страницам × 2 темы**. Нашёл и исправлено: 11px-подписи шапок таблиц (`--text-faint` мерился против `--surface`, а живёт на `--surface-hi` — тёмная 4.35, светлая 3.95 → переставлены по худшему месту) и цветные бейджи светлой темы (цвет на своём `*-soft`-тинте: 3.75–4.28 → затемнены `--accent`/`--success`/`--error`/`--warn`/`--text-muted` + тинты + legacy + тема Vuetify). Итог: **0 нарушений на 14 страницах в обеих темах**. ⚠ Не чинил как решения, а не недоделки: (1) белый на фирменном `#3B9EFF` — 2.79 на всех залитых primary-кнопках тёмной темы (дореформенное; лечится затемнением фирменного синего либо отдельным тоном для залитых поверхностей); (2) комментарии в блоках кода 3.05–3.47 — палитра подсветчика. **Седьмая итерация — блок кода понимает обе темы, заглушка снята:** `useHighlighter` переведён на **пару** тем (`github-light-default`/`github-dark-default`) + `defaultColor: false` — shiki не пишет свой цвет, каждый токен несёт `--shiki-light`/`--shiki-dark`, выбор делает CSS в `CodeBlock.vue` (тёмная ветка безусловная, светлая под `html[data-theme='light']` — зеркально объявлению токенов). Следствие: **смена темы не перерисовывает блоки** (разметка идентична до и после — проверено). Плашка осталась на токене приложения, а не на фоне палитры (два серых читались бы как шов). Костыль из пятой итерации (переобъявление токенов на `.code-block`) удалён. Палитры `-default` заодно закрыли долг по контрасту: **0 токенов ниже AA в обеих темах** (11 блоков / 7 языков на фикстуре + 7 блоков на Laravel-теле), все четыре варианта компонента, номера строк и копирование проверены в обеих темах; из семи тем-чанков грузятся ровно два. ⚠ Вид кода в тёмной теме (она по умолчанию) видимо изменился. Плюс по замечанию пользователя панель сделана **однородной**: тело меряется чистым белым, единственной залитой областью была шапка (`--surface-hi`) — блок читался как серый колпак без подложки; шапка переведена на `--surface`, разделитель — линейка, ховер кнопок поднят на `--surface-hi` (иначе совпал бы с шапкой). `vue-tsc` 0, `vite build` ok (15 `@font-face` / 12 `unicode-range` в бандле), живая проверка на :22040 в изолированном браузере, консоль чистая; невыбранные семейства не качаются вовсе. ⚠ `MarkdownRenderer.vue` затронут и этой задачей, и незакоммиченной миграцией на markdown-it — разделить нельзя. |
| [2026-08-27-markdown-renderer-markdown-it.md](2026-08-27-markdown-renderer-markdown-it.md) | 2026-08-27 | **Completed.** Переезд рендера markdown с marked v18 на **markdown-it 15** по итогам ресёча. Пайплайн вынесен в `web/src/components/markdown/render.ts` (markdown → санитизированный HTML, без Vue), компонент оставил себе подписи пилюль, клики и CSS. `html:false` (сырой HTML больше не доходит до санитайзера), linkify без fuzzy (иначе `app.py`/`README.md` становятся ссылками). Паритет кастомизаций: core-правила `entity_refs` (пилюли `TYPE@hash`, не трогает код-спаны и подписи ссылок) и `task_list_checkboxes` (у markdown-it их нет), рендер-правила fence/code_inline/link_open/image. **Починены таблицы** — в allowlist добавлены `div table thead tbody tr th td` + `s`, написан CSS (скролл-обёртка, липкая шапка, зебра, классы выравнивания вместо `style`). Проверка без браузера: модуль собран локальным esbuild и прогнан в node с jsdom — 11 сценариев (таблицы+выравнивание, экранирование `<script>`/`<iframe srcdoc>`, `javascript:`/`data:`/`vbscript:` → ссылка не создаётся вовсе, пилюли, чек-листы, зачёркивание, картинки, внешние/внутренние ссылки, breaks) + реальное тело 44 КБ: 23 таблицы, 66 пилюль, парс 16 мс. Демо design-system расширено, доки `frontend/markdown-rendering.md` переписаны, `web/dist` пересобран. Чанк рендерера 13.8 → 50.1 КБ gzip (ленивый), index не изменился. Живой прогон в браузере (dev :22040) по фикстуре RESEARCH@ef8a7d2f258de68b188bda («Test: полный набор markdown-разметки», только dev-БД) нашёл 4 вещи, которых не видно из node: выравнивание колонок не применялось (`.md-align-*` проигрывало по специфичности `.md-table td`), таблица с прозой растягивалась до 2186px из-за `max-content` (лечится `max-width: 60ch` на ячейках), блоки кода выглядели стопкой синих плашек (глобальное правило `code` рисует чип на каждой строке — дефект был и на marked), у чек-листа оставался маркер рядом с чекбоксом (теперь `li.md-task`). Липкая шапка таблицы выброшена: внутри `overflow-x: auto` она не липнет. Отдельным этапом починено определение языка в `useHighlighter.ts`: жёсткий список из 13 языков сужал всё **до** shiki, поэтому ` ```js `/` ```sh `/` ```py `/` ```yml `/` ```JSON ` падали в plain text. Теперь язык нормализуется и ищется в `bundledLanguages` самого shiki (алиасы включены), грамматики грузятся по требованию (по одному промису на язык) вместо тринадцати сразу, неизвестный id по-прежнему деградирует в `text`. Проверено в node и в браузере: дизайн-система тянет 5 языковых чанков вместо 13, все 8 демо-блоков подсвечены, консоль чистая. Затем блоки кода в телах переведены на **компонент**: fence больше не рендерится в HTML — парсер возвращает `{html, codeBlocks}` и оставляет `.md-code-slot[data-code-index]`, куда `MarkdownRenderer` монтирует `CodeBlock` через standalone `render()` (vnode получает `appContext`, слоты размонтируются перед подменой `v-html` и на unmount). Тела получили подсветку, бейдж языка, нумерацию строк и копирование. Добавлен вариант **`compact`** для однострочного кода (без шапки, кнопка копирования по наведению) — рендерер выбирает его автоматически; вариант добавлен и в демо дизайн-системы. Проверено в браузере на фикстуре: 11 слотов → 11 компонентов, 4 компактных (включая вложенные в список и в цитату), консоль чистая; попутно найдено и починено — код внутри курсивной цитаты наследовал курсив. Дальше: dual-тема и `shiki/core`. |
| [2026-08-26-markdown-renderer-research.md](2026-08-26-markdown-renderer-research.md) | 2026-08-26 | **Completed.** Ресёч через MCP `urb-research-stable` по теме «машинка рендера markdown»: RESEARCH@8913d13581d04a807ff59f, 7 областей (ландшафт парсеров / кастомизация и рендер в Vue-компоненты / безопасность / качество вывода / стриминг и производительность / когда оправдано своё / неочевидные решения — djot, Markdoc, MDC, smd, wasm, Expressive Code), 68 источников — все отревьюены (57 kept, 11 filtered), тела написаны по всем областям + итог. Вывод: парсер — чужой (marked → markdown-it, `html:false` по умолчанию + настоящие точки расширения), рендер-слой — свой (токены → VNode вместо `v-html`), DOMPurify остаётся вторым рубежом, shiki — fine-grained; свой парсер не писать. ⚠ У прогона пустые тела источников (`body: null`) — фетч контента не отработал; ключевые страницы дочитаны вручную, github/npm недоступны из песочницы. Кода не менял. |
| [2026-08-26-typography-research.md](2026-08-26-typography-research.md) | 2026-08-26 | **Completed.** Исследование типографики и системы отступов фронта через MCP urb-research-stable — `RESEARCH@2a0d3e1d7e626c8bc4baf0`, **8 областей, 76 источников** (51 kept / 25 filtered), 3 заметки. Кода не менял. Находка: размеры назначаются **тремя несогласованными источниками** (`$typography` Vuetify 4.0.7 / тег-стили `main.scss` / `.md-body` в `MarkdownRenderer.vue`) → три «основных» кегля 12/13/14px и немодульная лестница заголовков; длинное чтение (тела исследований) — 14px без `max-width`, строка до ~170 знаков при норме 45–75. План по приоритету: markdown (70ch + 16px + отступы заголовков) → токены шкалы + `$typography` → APCA-проверка контраста (WCAG 2.x непригоден для тёмной темы) → `--space-*` → стили md-таблиц → preload/метрический фолбэк шрифта. **Второй заход (неочевидные приёмы):** `text-box: trim-both cap alphabetic` + отступы в `lh` — написанные отступы становятся видимыми, подгонка на глаз исчезает; `size-adjust` на `@font-face` моноширинного вместо ручных `font-size: 0.875em` у кода везде; вывод `--text-muted`/`--text-faint` из фона через OKLCH relative color; `pow()` для шкалы одним `--ratio`; `text-wrap: balance/pretty`; `1cap` для иконок; `font-synthesis: none` как диагностика. Отклонены `margin-trim` и `hanging-punctuation` (не Baseline). ⚠ `query_search_run` вернул источники с пустым `body` при живом поиске (`fetch_error` пуст) — ключевые страницы читал напрямую, метрики Vuetify снял из установленного пакета. |
| [2026-08-26-dev-port-and-mcp-config.md](2026-08-26-dev-port-and-mcp-config.md) | 2026-08-26 | **Completed.** Изоляция dev-инстанса от stable по порту + конфиг подключения MCP. Порты уже разведены (`SERVER_PORT=22040`/Vite 22041 против stable 22020), оба свободны, токены у каждого чекаута свои — правки `.env` не потребовалось. Бекенд поднят вживую на :22040 (health 200, шесть модулей, перенесённая база), конфиг снят не руками, а из самого приложения (`GET /internal/core-mcp/servers/research` → `_stdio_config`, 23 тула) и отдан пользователю под ключом `urb-research-dev`. ⚠ Флаг: `AGENTS/tools/stop-all.sh` строкой `pkill -f 'app\.py'` убьёт и stable-инстанс (не заскоуплен на корень проекта). |
| [2026-08-26-import-db-from-stable.md](2026-08-26-import-db-from-stable.md) | 2026-08-26 | **Completed.** Перенос dev-базы из соседнего чекаута `urb-research-stable` (там живой бекенд на :22020 + stdio-шимы). Схемы идентичны (те же alembic-головы `com_005`/`wsm_003`/`rem_005`, тот же набор ключей настроек) → копия без миграций. Копировал **backup-API SQLite** с read-only подключения, не `cp` (источник пишется живым бекендом); `integrity_check`/`foreign_key_check` ok, старая база в `runtime/dev/app.sqlite3.bak-20260826-213822`. Итог 38.8 МБ: research 40 / area 291 / note 231 / source_query 1059 / source_document 5311 (kept 3268, filtered 1954, **pending 89**) / web_search query 1168, page 4331, result 5893; ключи коннекторов приехали. `migrate check` — up to date. Это снимок на 21:38, не связь: stable продолжает писать в свою базу. |
| [2026-08-26-purge-donor-project-traces.md](2026-08-26-purge-donor-project-traces.md) | 2026-08-26 | **Completed.** Вычищены все следы донорского проекта, из ядра которого форкнут репозиторий (имя пакета/бренд/бинари/пути симлинков/имена БД/фикстурные креды). Фан-аут из 4 саб-агентов по непересекающимся зонам: доки+инструкции (6 файлов, вкл. переписанную шапку `agent-primary.md` и снятое противоречие «SPA раздаёт nginx» → `mount_spa`), память (5), задачи+планы (21), код+конфиги (18: имя пакета, FastAPI `title`→`Uroboros.Research`, `.env*`, `.gitignore`, 10 run-конфигов IDE, фикстуры тестов, демо-строки DS + те же литералы в собранных чанках). Симлинк на донорский репозиторий удалён. Контрольный grep по дереву — ноль. `uv.lock` держал старое имя корневого пакета → ломал **любой** `uv run` (нет сети для `uv lock`), поле поправлено руками, `uv run` проверен. Тесты **446 passed**. `asyncio.Semaphore` не тронут. На пользователе: ренейм модуля/SDK в PyCharm, `pnpm --dir web build`, `uv lock` при сети. |
| [2026-08-26-project-onboarding-study.md](2026-08-26-project-onboarding-study.md) | 2026-08-26 | **Completed.** Изучение проекта: записи (MEMORY/tasks/docs/tools), дерево `src/` (роли `app.py`, состав модулей), модуль `research` вглубь (MCP-инструкции + 23 тула, кодек `TYPE@hash`, 5 моделей), обзор `web_search`. Прогон `uv run pytest -q` — **446 passed**. Кода не менял. Находки: `MEMORY.md` превышает лимит (30.1 KB) и грузится частично; у `research` нет doc-хаба `docs/research/INDEX.md`; в таблице «In work» висят строки, помеченные Completed в тексте. |
| [2026-07-09-public-repo-secret-audit.md](2026-07-09-public-repo-secret-audit.md) | 2026-07-09 | **Completed.** Аудит перед первым публичным коммитом. `.gitignore` корректен (реальный `.env`/БД/логи/secrets/`.claude`/`.idea` игнорируются). Удалены реальные Tavily/Firecrawl ключи из task-заметки 07-02 (отзыв/ротация — на пользователе). Симлинк `AGENTS/obsidian` → в `.gitignore` (диск не тронут). Клиентские email (trace3/eplus/24x7) в демо design-system → `acme.example`. Внутренние абсолютные пути (store-mount + домашние с username) и прод-хостнейм → плейсхолдеры (README + доки + task-файлы). `web/dist` пересобран. **Второй проход по AGENTS/+все задачи:** удалены реальные логин-креды разработчика (4 места) и пароль тест-БД → менеджер секретов/CHANGE_ME. **Третий проход (всё дерево + `dev/`):** аудит-записи, цитировавшие секреты дословно, отредактированы; username-путь вычищен из `dev/.run/*.xml` (5), `compare_scrape.py`, `overview.md`; бенч-`tmp/*.json` игнорируются, бенч-`.py` берут ключи из env. Прочее чисто (нет vendor-ключей/hash/webhook/JWT/приват-ключей/DSN). На пользователе: ротация Tavily/Firecrawl + смена засвеченного пароля. |
| [2026-07-08-mcp-connection-config-stdio.md](2026-07-08-mcp-connection-config-stdio.md) | 2026-07-08 | **Completed.** Страница `/mcp-servers`: вывод конфига подключения актуализирован под лёгкую stdio-обёртку (`app.py --mcp-stdio`, форма README) вместо remote-HTTP (`npx mcp-remote`/http+токен). Бэкенд `core_mcp/api.py` — `_stdio_config` (`uv run --directory <root> … --mcp-stdio`, ключ=код, `env.MCP_STDIO_CODE` только при >1 сервере), DTO `connection_configs` dict→`connection_config` str; фронт — убран переключатель клиентов (stdio одинаков для Desktop/Code), `api.ts`/view/locale. `web/dist` пересобран. Проверено вживую (порт 12299). `vue-tsc` 0, `--core` 285. Живой :12200 подхватит после рестарта. |
| [2026-07-08-connector-settings-token-links.md](2026-07-08-connector-settings-token-links.md) | 2026-07-08 | **Completed.** У коннекторов на `/settings/modules`: в описания `*_api_key` добавлены markdown-ссылки на страницы выпуска токенов (Tavily/Firecrawl/xAI/OpenRouter/OpenAI/Anthropic). Рендер ссылок в описаниях полей настроек **централизован** в `SettingField.vue` через `MarkdownRenderer` (убраны per-editor `:hint`/`persistent-hint`→`hide-details="auto"`, Bool/List/module-desc тоже); `MarkdownRenderer` открывает внешние ссылки в новой вкладке (`target="_blank" rel="noopener"`). `web/dist` пересобран. Проверено вживую (порт 12299, изолир. контекст): href/target корректны. `vue-tsc` 0, core_connectors 15, `--core` 285. Живой :12200 подхватит после рестарта бекенда. |
| [2026-07-08-web-scrapper-fetch-engine.md](2026-07-08-web-scrapper-fetch-engine.md) | 2026-07-08 | **Completed.** Локальный демон `daemon-web-scrapper` (порт 19020) как фетч-движок контента в web_search (код `web_scrapper`). Коннектор `core_connectors/services/web_scrapper` (`WebScrapperGateway.scrap_batch` → `POST /api/1.0/scrap-batch`, `BASE_URL` хардкод, auth опц. — Bearer только при заданном `web_scrapper_api_key`, пустой ключ = без auth; `HAS_BALANCE=False`, в реестр /connectors НЕ регистрируется). Настройки `web_scrapper_gateway_enabled`+`web_scrapper_api_key`. Фетч-движок `web_search/providers/web_scrapper` (`WebScrapperEngine`, `code=web_scrapper`, `pages_per_request=10`, только `fetch_engines`), опция `web_scrapper` в настройке `fetch_engine`. Тесты +2 (маппинг url→content), правлен test_settings; доки обновлены. `--module=web_search --module=core_connectors` **74 passed**. ⚠ Демон был stopped — живой e2e не прогонялся. |
| [2026-07-08-launcher-wake-daemon.md](2026-07-08-launcher-wake-daemon.md) | 2026-07-08 | **Completed.** MCP stdio-шим `--mcp-stdio`: клиент спавнит процесс по stdio → лениво поднимает backend (`app.py --backend`, detached, переживает шим) + системный браузер на главную, мостит вызовы на `/mcp/<code>` через `create_proxy`. В покое не крутится ничего; гасят вручную. Файлы: `src/apps/app/mcp_stdio.py`, роль в `src/app.py`, `Config.mcp_stdio_*`, env, run-config, доки `platform/mcp-stdio-shim.md`, тесты (`--core` зелёный, живой smoke = 23 тула research). Клиенту сменить `url:` → `command`-запись. |
| [2026-07-06-research-mcp-returns-and-nonnull-text.md](2026-07-06-research-mcp-returns-and-nonnull-text.md) | 2026-07-06 | **Completed.** Проработка research MCP-поверхности по методам + политика текстовых полей. (1) Даты-граница `core/utils/date.py`: `datetime_to_agent`/`datetime_from_agent` (формат `AGENT_DATETIME_FORMAT`, `DatetimeUTCStr` через них, `_to_utc` убран). (2) Возвраты тулов: `research_start`→**только код** (`ResearchCreated`), `research_update`→`{code,title,description}` **без дат** (`ResearchScan`), `research_get`→`ResearchView` (порядок code/title/description/body/areas/notes/created_at/updated_at; **queries убран**, **notes чинён** — тул их не грузил; вложенные `AreaScan`/`NoteScan` = code/title/description/updated_at, сорт по `updated_at`↑ `_oldest_first`); web-API DTO не тронуты (у MCP свои lean-DTO). (3) **Все свободно-текстовые поля** (description/body/summary/note) во всех таблицах research → **NOT NULL default `''`** (пустота==не задано, флаг заполнения не нужен): модели (`Mapped[str]`, `default=""`+`server_default=text("''")`), склеенные миграции `rem_001..005` **правлены in-place** (`nullable=False, server_default=sa.text("''")`), CRUD coerce `None→""` (`_clip`+создатели/сеттеры), DTO `str|None`→`str=""` (web_search-join поля url/domain/title/page.body остаются nullable). relevance(int)/error — nullable, не тронуты. (4) **Пересборка dev** (по явному согласию: правка create-миграций + rebuild): stop-all→бэкап 18 строк настроек→дроп (ассерт путь+хедер)→migrate upgrade (13 таблиц)→restore→restart (health ok, engines живы, migrate check чист). Проверки: throwaway-sqlite (omitted→`""`, PRAGMA NN default `''`), MCP-смоук (title-only → все текст-поля `""`), `--core`+web_search **323 passed**. (+) `research_list` причёсан: `ResearchListItem` (code/title/description/`updated_at`, без created_at), crud сорт по `updated_at`↓. (+) **Ренейм всех 23 тулов** в полную схему `research_<entity>[_s]_<verb>` (research_create/research_area_create/research_areas_list/research_note_*/research_query_create/research_source_*/…; research_get/list/update уже подходили); переименованы fn-имена `@mcp.tool`+кросс-ссылки в докстрингах+`_INSTRUCTIONS`+канвас, CRUD-слой не тронут (внутренний); проверено `list_tools`→23 новых имени. **(5) Вся схема `research-mcp.canvas` перенесена в код** (после долгой канвас-first проработки): финальные 23 тула — ренейм note/source/query без `research_`; новые `research_delete`/`area_delete`/`query_search_delete` (явный каскад — sqlite FK-каскад off), `source_review` (keep\|filter+relevance), `body_edit`/`body_add` (`services/body.py`, диспетч RESEARCH@/AREA@/NOTE@); убраны source_create/keep/filter/set_*; `query_search_run` зовёт web_search `Searcher.search` (блокирующе, наполняет источники — обвязка проверена живьём, сам прогон упал на tavily-коннекторе = конфиг env); `sources_list(code,status?)`/`query_search_list(code)` диспетч по префиксу; DTO source ужаты (`{code,status,url,title,summary,note,relevance,updated_at}`), `+BodyView`, `−QueryCreated/SourceCreated`; дроп колонки `research_source_query.body` (модель+`rem_004`+пересборка dev); `notes_list(kind?)`; `_INSTRUCTIONS` переписаны; web-API поправлен. **323 passed**, dev пересобран (health ok, migrate check чист). **(6) Тест-набор research-модуля** (был пуст): `tests/modules/research/` — **66 тестов** на все 23 тула (happy-path + сценарии ошибок, реальные записи в in-memory БД через тулы); `conftest` с фикстурами `db`/`mcp`(fastmcp-клиент)/`call`/`use_search`(stub веб-поиска); проверяются явные сообщения об ошибках (ToolError). Полный прогон `--core`+research+web_search **389 passed**. Осталось: rank-сорт внутри прогона, doc-hub `docs/research/`. |
| [2026-07-06-move-codec-to-research-at-separator.md](2026-07-06-move-codec-to-research-at-separator.md) | 2026-07-06 | **Completed.** Две связки: (1) сепаратор кода `_`→**`@`** (wire = `type@hash`, читается как namespaced-ref, в хеше не встречается); (2) **префиксы — только research** (единственный модуль с MCP-поверхностью). Кодек `mv core/utils/codes.py → modules/research/codes.py`; `research/constants.py` держит все 7 слов (вкл. ссылочные `SEARCH`/`PAGE`). **web_search больше не тегирует** — `prefixed`/`SearchCode`/`PageCode` убраны из моделей (код = `str`), `strip_prefix` из api, префиксы из constants → его API отдаёт голый хеш; research сам тегирует `SEARCH@`/`PAGE@`, когда ссылается (research→web_search dep не даёт web_search импортить кодек). Свапнуты импорты research (api + 4 mcp), все wire-примеры `PREFIX_…`→`PREFIX@…` (mcp-инструкции/докстринги/комменты/`wsm_001`), core `hashing.py` докстринг отвязан от модуля. Тесты web_search (голые ассерты, убран `strip_prefix`-импорт); память `codes_bare_storage_prefix_boundary`+MEMORY-строка; `docs/web_search/INDEX`; канвас research-tables (`@`+research-owned). Проверки: `--core`+web_search **323 passed**; codec-smoke (`RESEARCH@abc`→`abc`, DTO json тегирует, python-mode голый); в `src/` не осталось `core.utils.codes`/wire-`_`; canvas JSON валиден. research тестов нет (черновой surface). Осталось: переписать research_list/create/get по спеке. | **Completed.** Склейка миграций до **одной create-миграции на таблицу** (финальная схема, без промежуточных alter/drop) + пересборка dev с сохранением `core_modules_settings`. research **14→5** (`rem_001_research` + новые `rem_002_area` без sort/brief / `rem_003_note` с 6 kinds / `rem_004_source_query` / `rem_005_source_document`; удалены 13 старых), web_search **4→3** (wsm_004 content→body влит в `wsm_002_page`; удалён); core (`com_001..005`) уже one-per-table — не тронут. Ветки независимы (кросс-модульных/core FK нет), 3 линейных head'а. Проверка склейки на **временной** sqlite: колонки всех 8 таблиц == модели (ALL MATCH), CHECK'и на месте. Пересборка dev: stop-all → бэкап 16 строк настроек (PK (module,key), без FK) → дроп файла (ассерт путь+sqlite-хедер) → migrate upgrade (14 таблиц) → restore настроек → restart-backend (health ok, `/web-search/engines` читает восстановл. `search_default=xai`, коннекторы живы). migrate check чист; `--all`-default 341 passed. Отложено: String(25)→22, фронт-вью, web/dist. |
| [2026-07-06-three-letter-code-prefixes.md](2026-07-06-three-letter-code-prefixes.md) | 2026-07-06 | **Completed (код; пересборка dev — gated).** Спор «2→3 буквы» вылился в лучшую архитектуру: **хранимый код = голый 22-hex хеш, префикс — только презентация, надевается на границе, в БД не хранится.** Убирает проблему ширины/миграций целиком (схема не тронута) и делает будущий ренейм префикса бесплатным. Префиксы — читаемые **слова** (ноль декодирования): RESEARCH_/AREA_/NOTE_/QUERY_(source_query)/SOURCE_(source_document)/SEARCH_(web query)/PAGE_(web page). **Кодек** `core/utils/codes.py`: `strip_prefix` (rpartition, идемпотентен на голом хеше) + `prefixed(p)` = `Annotated[str, PlainSerializer(when_used=json)]` (тегает json-форму поля; python-`model_dump()` остаётся голым → паттерн model_dump-respread безопасен). CRUD генерит голые коды (`random_hash`/`text_hash`, `*_CODE_PREFIX`-конкатенация убрана), остаётся чистым. Выход тегируется на **общих DTO** (покрывает и MCP, и HTTP-API одним местом); вход — явный `strip_prefix` на каждом code-параметре тулов + path-параметре API (детерминированно). Колонки остаются `String(25)` (голый код=22, слак безвреден) → **миграций нет**. Проверки: e2e через fastmcp-клиент (выход `RESEARCH_…`, тегированный код на входе резолвится, вложенные коды в Detail тегированы), FastAPI-тесты (`SEARCH_…`), `--core`+web_search 323, `vue-tsc` 0 (фронт — коды непрозрачны, литералов нет), app build, migrate check без дрейфа. Обновлены docstring'и/MCP-инструкции/бенч/канвас. **Осталось (gated):** пересборка dev — стоп backend → чистка строк research+web_search (FK-порядок) → `com_*` целы → migrate upgrade → рестарт (старые строки с префиксными кодами двойно тегаются на чтении). Отложено: `String(25)`→22, фронт-вью, web/dist. |
| [2026-07-04-research-registry-module.md](2026-07-04-research-registry-module.md) | 2026-07-04 | **In work.** New domain module `research_registry` — оркестратор + реестр ресёрча, выставляет research-MCP поверх CRUD (эталон `conversation_insights`). Step 1: болванка. Step 2 (черновой сквозной MCP по схеме канвасов): models `research→research_query→research_document`/`research_report` (мягкие ссылки на web_content) + constants + crud + dto + `mcp/` сервер `research-registry` (11 тулов, 3 поверхности) + снят блокер MCP-auth (`Config.mcp_token` ENV + черновой `mcp_token_resolver`). Смоук через in-memory `Client` (весь поток), `--core` 266 passed, dev-схема через `create_all`. Дальше: services (оркестрация), api+фронт, миграции, auth-модуль. |
| [2026-06-27-research-pipeline-design.md](2026-06-27-research-pipeline-design.md) | 2026-06-27 | **In work.** Design the work pipeline + DB table structure for the research/search MCP server; capture as an Obsidian Canvas in `AGENTS/obsidian`. Design-only — no code yet. |
| [2026-06-27-strip-auth-from-core-shell.md](2026-06-27-strip-auth-from-core-shell.md) | 2026-06-27 | **In work.** Post-module-strip sweep: purge stale old-module prose/dangling links from `AGENTS/`, and remove the whole auth/permissions/users stack from `web/src` (local site, no auth) — auth store, CASL, login/forbidden views, /me bootstrap, 401/403 redirects, permission-gated nav/settings. |
| [2026-07-06-connector-balance-and-limits.md](2026-07-06-connector-balance-and-limits.md) | 2026-07-06 | **In work.** Определить баланс/лимиты каждого коннектора (Tavily/Firecrawl/xAI) через API. Живьём снят реальный shape read-эндпойнтов бенчем `dev/bench/core_connectors/run_balances.py` (сырьё в `tmp/balance_*.json`): **Tavily** `GET /usage` (остаток = `plan_limit−plan_usage`); **Firecrawl** три эндпойнта — credit-usage + token-usage (**две оси**) + queue-status (concurrency); **xAI** остатка через API НЕТ (только `acls` + пер-модельные цены, расход пост-фактум в `cost_in_usd_ticks`). Дальше: методы `token_usage()`/`queue_status()` на FirecrawlGateway + нормализованный интерфейс баланса (список осей `[{axis,remaining,limit,unit,period_end?}]`; xAI → пусто). |
| [2026-07-04-core-gateway-module.md](2026-07-04-core-gateway-module.md) | 2026-07-04 | **In work.** New infra module `core_gateway` — thin connectors to external APIs + their creds as runtime settings. Step 1: empty module + `tavily_api_key`/`firecrawl_api_key` secret settings (no registry). Step 2: Tavily service `services/tavily/` (constants+params+gateway) with a param model per request (search/extract/map → native JSON). Registered in `build_modules`; `--core` 265 passed. Rest (Firecrawl service, web_content rewiring, key migration, tests) step-by-step. |

## Completed

| File | Date | Description |
|------|------|-------------|
| [2026-08-26-research-readable-cyrillic-fonts.md](2026-08-26-research-readable-cyrillic-fonts.md) | 2026-08-26 | **Completed.** Ресёрч через urb-research-stable: универсальные шрифты для лёгкого чтения с кириллицей, open-source/некоммерческие. `RESEARCH@1b464e08943f77787fbc11` — 6 областей, 13 запросов, 79 отревьюенных источников, 5 заметок. Итог: ролевой OFL-набор (Golos Text/UI + Literata + JetBrains Mono ⎪ IBM Plex ⎪ PT). Ключевое: «самого читаемого шрифта» нет, шрифты «для дислексии» не работают, а Atkinson Hyperlegible / Luciole / B612 **без кириллицы** (проверено по `google/fonts` METADATA.pb) → замена SIL Andika. ⚠ У всех источников прогона `body` пустой при статусе `pending` (не `fetch_error`) — возможный дефект фетча. Код не трогался. |
| [2026-07-09-settings-bool-field-description-grouping.md](2026-07-09-settings-bool-field-description-grouping.md) | 2026-07-09 | **Completed.** Browser-QA `/settings/modules`: описание bool-тумблера коннектора висело ~28px под высоким VSwitch (min-height 40px) как оторванное, читалось между тумблером и полем ключа. Фикс: описание bool-поля рендерится **внутри** `SwitchPanel` (default-слот → `switch-panel__desc`, `MarkdownRenderer` — ссылки сохранены) в `SettingFieldBool.vue`; общая подпись снизу подавляется для bool в `SettingField.vue` (`descriptionBelow = Boolean(desc) && kind !== 'bool'`). Теперь коннектор читается блоком [тумблер+описание]→[ключ+описание]. `web/dist` пересобран (vue-tsc 0). Проверено вживую (:12200 после rebuild): gap 28→2px, не-bool поля не тронуты. Живой :12200 уже подхватил (dist). |
| [2026-07-06-anthropic-connector-spend.md](2026-07-06-anthropic-connector-spend.md) | 2026-07-06 | **Completed.** Anthropic (Claude) коннектор — **только расход** (остатка нет). `AnthropicGateway(ServiceGateway)`: метрика «Расход 30д» через Admin `GET /organizations/cost_report` (bucket 1d, ⚠ `amount` — строка в **центах** → `Σ/100`), Admin-ключ `anthropic_admin_api_key` (`sk-ant-admin…`). ⚠ **Auth иная** — `x-api-key`+`anthropic-version`, не Bearer → база вынесла авторизацию в переопределяемый `_auth_headers()`. Настройки+реестр (6 коннекторов); фронт/dist не менялись. Тесты +2 (14). `--module=core_connectors` 14 / full **343 passed**. ⚠ Живая сверка pending — нужен Admin-токен Anthropic. |
| [2026-07-06-openai-connector-spend.md](2026-07-06-openai-connector-spend.md) | 2026-07-06 | **Completed.** OpenAI-коннектор в core_connectors — **только расход** (остатка баланса у OpenAI через API нет; research openai/anthropic записаны). `OpenAIGateway(ServiceGateway)`: метрика «Расход 30д» через **Admin Costs API** `GET /organization/costs` (bucket 1d, `Σ results[].amount.value`, доллары), Admin-ключ `openai_admin_api_key` (`sk-admin-…`; обычный → 403). База получила `_request(params=…)` для query. Настройки+реестр (5 коннекторов); фронт/dist не менялись (data-driven). Тесты +2 (12). `--module=core_connectors` 12 / full **341 passed**. ⚠ Живая сверка pending — нужен Admin-токен от пользователя. Дальше: Anthropic (расход, `amount` в центах-строке). |
| [2026-07-06-note-kind-clarification.md](2026-07-06-note-kind-clarification.md) | 2026-07-06 | **Completed.** Добавлен 6-й тип заметки `research_note.kind` — `clarification` (уточнение/ограничение **от пользователя**: scope/приоритеты/определения; в отличие от `decision` — выбор самого агента). Правки: constants (NOTE_CLARIFICATION + NOTE_KINDS), миграция rem_014_note_kind_clarification (forward-only, расширяет CHECK `ck_research_note_kind` с 5→6 через batch_alter_table, down сужает), mcp/note.py (доки note_add/note_update) + mcp/__init__ инструкция Notes; канвас. Проверки: migrate upgrade/check, read-only DDL CHECK = 6 значений (имя сохранено), import-smoke, `--core` 266. |
| [2026-07-06-area-conform-title-desc-body.md](2026-07-06-area-conform-title-desc-body.md) | 2026-07-06 | **Completed.** research_area приведена к общему паттерну артефакта **title/description/body** (как research_index/research_note) — убран бриф-набор колонок objective/scope/expectations (вариант A). Бриф (цель/границы/формат) теперь — **инструкция в промпте area_add**, агент раскрывает его в body. Правки: constants (AREA_BRIEF_MAX), models/area.py (3 колонки + docstring), crud/area.py (create/update params + import), dto AreaDetail=body, mcp/area.py (area_add=title/description, area_update=title/description/body, бриф в docstring) + mcp/__init__ секция Areas; миграция rem_013_area_drop_brief (forward-only, down пере-добавляет String(1024)); канвас. Smoke (бриф-полей нет, create/update body, mcp build); `--core` 266; `vue-tsc` 0; PRAGMA area = code/research_code/title/description/body/dates. |
| [2026-07-06-area-drop-sort.md](2026-07-06-area-drop-sort.md) | 2026-07-06 | **Completed.** Убрано поле `sort` у research_area (не нужно) — области сортируются по `created_at`. Правки: models/area.py (колонка + Integer-импорт), crud/area.py (create/update/list order), dto AreaRow, mcp/area.py (area_add/area_update params + doc) + mcp/__init__ инструкция; миграция rem_012_area_drop_sort (forward-only, down пере-добавляет); канвас research-tables.canvas. Smoke (create без sort, list по созданию, update body); `--core` 266; `vue-tsc` 0; PRAGMA без sort. |
| [2026-07-06-openrouter-connector.md](2026-07-06-openrouter-connector.md) | 2026-07-06 | **Completed.** OpenRouter добавлен как коннектор в core_connectors — **только баланс** (не инференс, полный провайдер не тянем). `OpenRouterGateway(ServiceGateway)`: `openrouter.ai/api/v1`, настройки `openrouter_gateway_enabled`+`openrouter_api_key` (runtime-секрет; ENV отложено). Баланс = **две метрики**: «Лимит ключа» (`/api/v1/key` usage/limit, %) + «Баланс» (`/api/v1/credits` total−usage, может быть минус); `/credits` best-effort. Регистрация в `connectors_registry`. Фронт/dist НЕ менялись (data-driven, 4-я карточка сама). Live: лимит 5.16/10 51.6% + баланс −0.16 USD; endpoint 4 коннектора. Тесты +2 (10). `--module=core_connectors` 10 / full **339 passed**. Ключ задать в Настройки→Сервисы. |
| [2026-07-06-rename-source-query-document.md](2026-07-06-rename-source-query-document.md) | 2026-07-06 | **Completed.** Ренейм двух «внешне-материальных» таблиц под под-домен **source** (документ=источник, «source document» — редундантность): research_query→**research_source_query** (источниковый запрос, +disambiguation с web_search_query), research_document→**research_source_document** (источник). Префиксы кодов RQ_/RD_ **не** трогали (отдельный шаг). Разделение имён: БД/модели/crud/dto — полный лист (ResearchSourceQuery/Document, source_query_*/source_document_*, *Row/SourceQueryDetail); агент-facing MCP-тулы — короткие (**source_**: source_add/get/keep/filter/set_relevance/set_summary/mark_fetch_error, param source_code; query_documents→query_sources; query_link/research_queries остались). mv 5 файлов (models/crud/mcp {source_query,source_document}); FK document.query_code→research_source_query.code; api пути /queries→/source-queries, /documents→/source-documents; бенч. Миграция rem_011_source_tables (forward-only, dev пуст → drop+recreate обеих с новыми именами constraint/index; down пересоздаёт старую пару). Smoke (RQ_/RD_, JOIN url/title, query body, mcp import); MCP 23 тула без дублей; `--core` 266; `vue-tsc` 0. Дальше: 3-буквенные префиксы, body-редактор (index/area/source_query/note), обновить канвас. |
| [2026-07-06-connector-balance-metrics-and-openrouter-bench.md](2026-07-06-connector-balance-metrics-and-openrouter-bench.md) | 2026-07-06 | **Completed.** Баланс коннектора → **список метрик** (`ConnectorBalance.metrics: BalanceMetric[]`): лимит ключа/фактический баланс/кредиты сосуществуют, выводим что доступно. `BalanceMetric` = `label` + деньги (`amount`/`currency`) ИЛИ «использовано из всего» (`used`/`total`/`used_percent`/`unit`), фабрики `money`/`usage`. Гейтвеи: tavily/firecrawl → метрика «Кредиты», xai → «Баланс». Фронт `ConnectorBalance.vue` на массиве метрик (строка на метрику: label+значение+бар). Плюс **упрощённый бенч-шлюз OpenRouter** `dev/bench/core_connectors/openrouter/run_balance.py` (только баланс, ключ из ENV) — сверено живьём: `/api/v1/key` usage 5.16/limit 10/remaining 4.84, `/credits` баланс −0.16 (может быть минус); research `openrouter/INDEX.md`. `--module=core_connectors` 8 / `vue-tsc` 0; `web/dist` пересобран. Дальше: OpenRouter в src (ENV-креды). |
| [2026-07-06-drop-report-add-query-body.md](2026-07-06-drop-report-add-query-body.md) | 2026-07-06 | **Completed.** research_report удалён (лишняя сущность) — синтез по запросу переехал в новую колонку **research_query.body** (Text, nullable). research_query из чистой «таблицы связей» стал «связь + вывод». Удалено: models/crud/mcp report.py, ResearchReport из __init__, ResearchReportRow/View из dto, report-поверхность MCP (report_build/report_get) + Report-секция инструкций, report_crud/ResearchReportRow из api. Добавлено: query_set_body(code, body) (писатель тела, переиспользует body-редактор). QueryDetail.report→body; get_query отдаёт body. Миграции rem_009_query_body (add column) + rem_010_report_drop (drop table, down пересоздаёт последнюю форму со string query_code). Правки wording report→«query synthesis» в document/research mcp; module.py docstring. Бенч: report_create→query_set_body. Тела теперь у **index/area/query/note** — все под будущий общий body-редактор. Smoke (body пишется/читается, report-модули ModuleNotFoundError, mcp_server собирается); `--core` 266; `vue-tsc` 0. Дальше в той же волне: 3-буквенные префиксы (+research_document→research_source/RSS_), body-редактор, обновить канвас. |
| [2026-07-06-research-note-entity.md](2026-07-06-research-note-entity.md) | 2026-07-06 | **Completed.** Новая сущность research_note (RN_) — **свободная рабочая память исследования**: то, что агент фиксирует по ходу и что не привязано к документу/области. Мини-артефакт с типовыми полями описания как у research_index (title≤128/description≤512/body Text) + обязательный **kind** («схема как промпт», без дефолта). 5 типов (NOTE_KINDS): result/idea/question/memory/decision; CHECK. Поля: code(RN_ PK) · research_code(FK research_index CASCADE, **без area-привязки** — отклонена) · kind · title/description/body · даты; index на research_code. Миграция rem_008_note (forward-only, поверх rem_007, **новая таблица — без пересборки**). crud (усечение title/description по code points, кириллица-safe; note_create/get/list/update/delete), dto (NoteRow/NoteDetail + notes в ResearchDetail), api (notes в детали + GET /notes/{code}), MCP (note_add/research_notes/note_get/note_update/note_delete, _require_kind до вставки; инструкции). Комплементарна области (структура вывода), не замена. Smoke (RN_, кириллица, CRUD, усечение 200→128, CHECK kind→IntegrityError); `--core` 266; `vue-tsc` 0. Отложено: фронт note/area-вью, 3-буквенные префиксы, web/dist, services/. |
| [2026-07-06-research-document-redesign.md](2026-07-06-research-document-redesign.md) | 2026-07-06 | **Completed.** research_document переделан в **ссылку + оценку** (RD_): не хранит копию страницы — url/domain/title/body **join'ятся** из web_search_page по page_code («join по умолчанию»); summary — локальный снимок. Поля: code(RD_ PK) · research/area/query_code(FK CASCADE) · page_code(soft-ref→WD_) · status · relevance(int 1–10) · summary · note · даты. unique(query_code,page_code); CHECK status + CHECK relevance 1–10. **4 статуса**: fetch_error/pending/kept/filtered (kept↔filtered — явное решение агента, relevance — отдельный балл; дубль=relevance 5+filtered). Убраны url/title/content/filtered/filter_reason. Миграция rem_007 (forward-only, dev пусто). crud read'ы джойнят WebSearchPage (research=потребитель), dto-билдеры document_row/detail. MCP: document_add(query_code,page_code,summary?), document_get(joined body), keep/filter/mark_fetch_error/set_relevance, убран set_content; бенч сидит web_search_page. Smoke (JOIN url/title/body, CHECK relevance, unique enforced); `--core` 266; `vue-tsc` 0; MCP 20 тулов. Один док в двух исследованиях: общая страница, изолированные строки — схема покрывает. Отложено: фронт, 3-буквенные префиксы, web/dist. |
| [2026-07-06-research-query-link-table.md](2026-07-06-research-query-link-table.md) | 2026-07-06 | **Completed.** research_query переделан в **таблицу связей** (RQ_): создаётся пост-фактум (поиск отработал, документы есть), привязывает research + area + web_search-прогон. 6 полей: code(25 PK) · research_code(FK→research_index) · area_code(FK→research_area) · search_code(soft-ref→WQ_, без FK) · query(Text) · created_at; unique(area_code, search_code). Убраны id/status/text/updated_at. Форс-ripple: research_document/research_report query_id(int)→query_code(String FK). Миграция rem_006_query_link (forward-only, dev пусто → drop+recreate трёх таблиц, БЕЗ пересборки). MCP: `research_add_queries`→`query_link(research_code,area_code,search_code,query)`, document/report query_id→query_code; constants очищены; бенч на новый флоу. Smoke (RQ_, привязки, unique enforced); `--core` 266; `vue-tsc` 0; MCP 20 тулов. Отложено: фронт query-вью, редизайн document/report, 3-буквенные префиксы, web/dist. |
| [2026-07-06-connectors-monitoring-page.md](2026-07-06-connectors-monitoring-page.md) | 2026-07-06 | **Completed.** Реестр коннекторов `services/registry.py` → **корень модуля** `registry.py` (домен ≠ раскладка `services/`). Новый internal `GET /connectors` (`api/connectors.py` → `list[ConnectorView]`, баланс вживую) + `module.py` роутер. Фронт-фича `features/core_connectors`: страница `/connectors` карточками (эталон TasksView — имя/описание/badge/баланс), компонент `ConnectorBalance.vue` на **массиве** DTO (деньги/кредиты+бар/ошибка), routes+locales, пункт нав «Сервисы» в разделе «Мониторинг» **первым перед «Задачи»**. Live `GET /internal/connectors` 200. Follow-up: кредиты в форме «использовано / всего» + поля `credits_total`/`credits_used_percent` в DTO (фабрика `from_credits`), бар использования (warn >85%). `--module=core_connectors` 8 / full **337 passed**; `vue-tsc` чист; `web/dist` пересобран. |
| [2026-07-06-connectors-registry-and-passport.md](2026-07-06-connectors-registry-and-passport.md) | 2026-07-06 | **Completed.** Реестр переосмыслен как реестр **коннекторов** (домен), не баланса: `BalanceRegistry`→`ConnectorsRegistry` (+`connectors_registry`), баланс — одна из граней. Паспорт на базе `ServiceGateway`: `LABEL`→`NAME`+новый `DESCRIPTION`. Два DTO (`services/balance.py`→`dto.py`): `ConnectorInfo` (service/name/description/enabled/has_balance) + `ConnectorBalance` (деньги/кредиты/error) + `ConnectorView`=info+опц.balance. Метод `connectors(*, with_balance=False)`. xAI = один коннектор: `XaiGateway.balance()` **делегирует** management (ленивый импорт). Тесты переписаны (8, pure). Живьём: tavily 983/17, firecrawl 898/102, xai $6.02. `--module=core_connectors` 8 / full **337 passed**. |
| [2026-07-06-research-area-table.md](2026-07-06-research-area-table.md) | 2026-07-06 | **Completed.** Новый средний уровень `research_area` (RA_): research_index → research_area → research_query. Область = знаниевый под-артефакт с двухслойным набором-брифом: скан (title 128 / description 512) + бриф (objective / scope / expectations — все 1024; убрано `constraints`, границы входят в scope) + body (Text) + sort. Размеры — **усечением в CRUD** `_clip` по code points (кириллица-safe, не ошибка). Первая **forward-only** миграция `rem_005_area` по новой политике — **без пересборки БД** (`migrate upgrade` добавил таблицу поверх, данные целы). Модель/миграция/crud/dto (AreaRow скан + AreaDetail полный; `areas` в ResearchDetail)/api (`GET /areas/{code}`)/mcp (`area_add`/`research_areas`/`area_get`/`area_update`, 20 тулов). Smoke кириллица-усечение (128/512/1024, символы целы; body без лимита); `--core` 266 passed; `vue-tsc` EXIT 0. Дальше: перевесить research_query на область (`direction_code`), фронт областей, `web/dist`. |
| [2026-07-06-connector-balance-dto-and-registry.md](2026-07-06-connector-balance-dto-and-registry.md) | 2026-07-06 | **Completed.** Единый слой баланса core_connectors: флаг базы `HAS_BALANCE` + `SERVICE`/`available()`, общий `balance()->ServiceBalance` (гейт + хуки `_fetch_balance`/`_parse_balance`), DTO `services/balance.py` (сумма `amount`/`currency` ИЛИ кредиты `credits_used`/`credits_available`, +`error`), реестр `services/registry.py::balance_registry.collect()` (параллельно по не отключённым, сбой→`error` в DTO). Умеют tavily/firecrawl (кредиты) + `XaiManagementGateway` (USD, инверсия знака); инференс-xai нет. Тесты `tests/modules/core_connectors/test_balance.py` (7, pure). Живьём: `tavily 983/17`, `firecrawl 898/102`, `xai $6.02`. `--module=core_connectors` 7 / full **336 passed**. |
| [2026-07-06-migration-policy-forward-only.md](2026-07-06-migration-policy-forward-only.md) | 2026-07-06 | **Completed.** Смена политики миграций: убрано прежнее правило «правка миграции in-place + пересборка dev». Новое: изменение схемы = **новая forward-only миграция** (наращиваем цепочку `add_*`/`drop_*`/`rename_*`), применённые миграции не редактируем; **очистка/пересборка dev-базы — только по явному требованию пользователя**. Переписаны MEMORY.md §3 (гоча про dev-sqlite), `docs/platform/database.md`, `docs/platform/migrations.md` (раздел «When to consolidate» → «new forward-only migrations»). 🔴-предупреждение об удалённом иноде и `migrations_dir` сохранены (применяются, когда пересборка всё же запрошена). Прочее (naming, one-table-per-creation, `depends_on`, «migrate upgrade после написания») без изменений. |
| [2026-07-06-research-index-content-fields.md](2026-07-06-research-index-content-fields.md) | 2026-07-06 | **Completed.** Рефактор research (итерация 2): `research_index` → знаниевый артефакт. Убран статус исследования (колонка + CHECK + `RESEARCH_*` константы; `research_query.status` не тронут). Вместо `topic`/`overview` — `title` (`String(128)`, NOT NULL), `description` (`String(512)`, nullable), `body` (`Text`, nullable). Правки сквозь constants/model/`rem_001` (без CHECK)/crud (`research_create(title,description?,body?)`, `research_set_overview`→`research_update`)/dto (убран `ResearchOverview`, `body` в `ResearchDetail`)/api/mcp (`research_start(title,…)`, `research_get`→Detail c body, `research_update`)/бенч/фронт (api/labels/стор/вьюхи/локаль: колонки name+description, деталь title+description+body). Dev-sqlite пересобран (backend был жив → остановлен по правилу удалённого инода → бэкап 12 настроек→drop→`migrate upgrade`→restore→**рестарт backend**, health 200). `migrate check` up-to-date; in-memory smoke; `--core` 266 passed; `vue-tsc` EXIT 0; live `/researches`→пусто. |
| [2026-07-06-services-base-gateway-and-xai-management.md](2026-07-06-services-base-gateway-and-xai-management.md) | 2026-07-06 | **Completed.** Вынесена общая HTTP-обвязка гейтвеев в `services/base.py::ServiceGateway` (подкласс = ClassVar-ы поверхности `LABEL`/`BASE_URL`/`API_KEY_FIELD`/опц.`TIMEOUT`/`ENABLED_FIELD`, `None`→гейт по ключу; мёртвый `code` удалён). Tavily/Firecrawl/Xai мигрированы на базу без смены имён/сигнатур. **xAI = два гейтвея:** `XaiGateway` (инференс) + новый `XaiManagementGateway` (биллинг `management-api.x.ai`, отдельный секрет `xai_management_api_key`, методы `prepaid_balance`/`spending_limits`/`invoice_preview(team_id)`, USD-центы). Бенч `run_balances.py` расширен (резолв `team_id` из inference). Живая сверка баланса — после ключа от пользователя. `--module=web_search --core` 323 / full **329 passed**. |
| [2026-07-06-research-index-code-pk.md](2026-07-06-research-index-code-pk.md) | 2026-07-06 | **Completed.** Рефактор research (итерация 1): корневая таблица `research`→`research_index`, PK int `id`→строковый код `code` `RS_<random_hash()>` (`String(25)`, схема кодов web_search `WQ_`/`WD_`, дедупа по теме нет). Дочерний FK `research_query.research_id:int`→`research_code:String(25)` (→`research_index.code`, CASCADE). Правки сквозь model/migrations (`rem_001/002` in-place, revision ids сохранены)/crud (`research_code()` генератор, `query_count_by_research_codes`)/dto/api (`/researches/{research_code}`)/mcp (тулы на `research_code`)/фронт (api/routes `:code`/сторы/вьюхи)/бенч. Dev-sqlite пересобран (бэкап 11 настроек→drop→`migrate upgrade`→restore). `migrate check` up-to-date; in-memory smoke ok; `--core` 266 passed; `vue-tsc` EXIT 0. Дальше: query/document/report → `RQ_`/`RD_`/`RR_`. |
| [2026-07-06-rename-core-gateway-to-core-connectors.md](2026-07-06-rename-core-gateway-to-core-connectors.md) | 2026-07-06 | **Completed.** Переименован инфра-модуль `core_gateway` → `core_connectors`: директория, класс `CoreConnectorsModule`, `name`, все импорты `src.modules.core_gateway`, докстринги (шлюз→коннектор), потребители (apps + web_search providers/base + прозаические ссылки), фронт (лейбл `core_connectors: 'Сервисы'`), бенчи `dev/bench/core_connectors/`, доки (`docs/core_connectors/INDEX.md` + web_search) и MEMORY routing. **Настройки перенесены** в dev-sqlite: `UPDATE core_modules_settings SET module='core_connectors'` (6 строк, ключи tavily/firecrawl/xai сохранены). Внутренние `XxxGateway`/`*_gateway_enabled` оставлены как есть. `web/dist` пересобран. Full **329 passed**. |
| [2026-07-05-timestamp-single-source.md](2026-07-05-timestamp-single-source.md) | 2026-07-05 | **Completed.** Единый источник времени — `utc_now()` вместо `func.now()` во всех таймстемп-колонках. Причина: в dev-sqlite смешанные форматы (`.000000` vs без) от двух источников — app-side `utc_now()` и DB-side `server_default=func.now()`/`onupdate=func.now()`. Свёл к `utc_now()` (Python-side `default`/`onupdate`), `func.now()` убран из моделей и миграций (на PG `func.now()` в наивный TIMESTAMP пишется в tz сессии БД, не UTC). Фактический охват — **web_search** (`wsm_001..003`); `research` уже был чист (модуль `research_database` параллельно переименован в `research`, там уже `utc_now`). Доки `dates.md`/`backend.md`. `grep func.now()` пусто; `web_search --core` **323 passed**; dev-sqlite пересобран (таймстемпы без DDL-дефолта, integrity ok, migrate check up-to-date). |
| [2026-07-05-web-search-page-title.md](2026-07-05-web-search-page-title.md) | 2026-07-05 | **Completed.** Заголовок документа — первоклассная колонка `web_search_page.title` (`String(512)`, nullable), а не поле в `query_result.meta`: заголовок — свойство страницы, не запроса (в отличие от контекстного `summary`). Заполняется из выдачи поиска при создании (дедуп не перетирает). `title` повышен до верхнего ключа контракта провайдера (как `summary`), из `meta` убран (там контекстное — у xAI `reason`); `QueryResultView.page_title` из джойна. Фронт минимум: `QueryView` `page_title`, `PageView` заголовок в шапке. `--module=web_search` **57 passed**; `vue-tsc` EXIT=0; docs+bench; dev-sqlite пересобран (`title` на месте, integrity ok). |
| [2026-07-05-web-search-fetch-engine-fill.md](2026-07-05-web-search-fetch-engine-fill.md) | 2026-07-05 | **Completed.** Заполнять `web_search_page.fetch_engine` при получении контента (колонка была, но пустовала). Движок снимается на попытке фетча — `pages_mark_processing(codes, *, fetch_engine)`: запись держится и при `done`, и при `error`. `searcher._fetch_pages` передаёт `fetcher.code`. Тесты `test_search` ассертят движок (в т.ч. при ошибке страницы). `--module=web_search` **57 passed**. Схема не менялась (колонка была) — доп. пересборка dev не нужна. |
| [2026-07-05-web-search-summary-and-xai-bench.md](2026-07-05-web-search-summary-and-xai-bench.md) | 2026-07-05 | **Completed.** Первоклассная колонка `web_search_query_result.summary` (Text, после `score`) — краткое содержание страницы в контексте запроса, общее для всех движков (Tavily `content`, Firecrawl `description`, xAI — новое поле строгого вывода `LINKS_SCHEMA`+`AGENT_INSTRUCTION`); `snippet` убран из `meta`, фронт `QueryView` читает `r.summary`. Миграция `wsm_003` правлена in-place + dev-sqlite пересобран (бэкап настроек→drop→`migrate upgrade`→restore). Полноценный верстак `dev/bench/web_search/xai/` (гоняет провайдер-адаптер `XaiSearchEngine`). `--module=web_search` 56 / full **328 passed**; `migrate check` up-to-date; `vue-tsc` EXIT=0; docs. |
| [2026-07-05-web-search-create-query-frontend.md](2026-07-05-web-search-create-query-frontend.md) | 2026-07-05 | **Completed.** Создание поискового запроса с фронта. Бэк: `POST /queries` (**fire-and-forget** через `Searcher.submit` — 202 + `pending` сразу, прогон в фоне `asyncio.create_task`; `_prepare`/`_run` вынесены из блокирующего `search`) + `GET /engines` (доступные движки по ролям + дефолты). Фронт: кнопка «Создать запрос» → `VDialog` (текст + выбор движка поиска/контента из `/engines` с преселектом дефолта + число страниц 1..50), стор `create`→обновление списка. i18n. `--module=web_search` 56 / full **328 passed**; `vue-tsc` EXIT=0; docs+memory. web/dist не пересобран (dev=Vite). |
| [2026-07-05-web-search-full-review-fixes.md](2026-07-05-web-search-full-review-fixes.md) | 2026-07-05 | **Completed.** Полная сверка web_search: тесты (51/323), схема-паритет, engine-рename сквозь бэк+фронт, `sort_by` end-to-end — актуально. Исправлены 3 находки: (1) док-факт «dev-sqlite строится create_all» → миграции; (2) классы `TavilyProvider`/`FirecrawlProvider` → `TavilyEngine`/`FirecrawlEngine` (последний остаток «provider»); (3) докстринг `models/page.py` per-provider→per-engine. Full **323 passed**. |
| [2026-07-05-web-search-frontend-datatable-sort.md](2026-07-05-web-search-frontend-datatable-sort.md) | 2026-07-05 | **Completed.** Фронт web_search подтянут к эталону conversations: серверная **сортировка по клику на заголовок** (`must-sort`/`@update:sort-by` → стор `sortBy`/`sortDir`) и даты в две строки (`fmtDateTime` + относительная `fmtRelative`). Бэк (без миграций): `sort_by` с белым списком колонок в `query_list`/`page_list` + эндпойнты `GET /queries`,`/pages` (неизвестный ключ → `created_at`, dict-lookup от инъекции). В таблицу страниц добавлена колонка «Создана». Убрана кнопка-тумблер + блок i18n `sort`. Тесты +2 (сортировка + фолбэк). `--module=web_search` 51 passed; `vue-tsc` EXIT=0; docs обновлены. `web/dist` не пересобран (нет pnpm). |
| [2026-07-05-web-search-searcher-class-engine-rename.md](2026-07-05-web-search-searcher-class-engine-rename.md) | 2026-07-05 | **Completed.** `searcher.py` → статический класс-фасад `Searcher`: публичные `search_engines()`/`fetch_engines()` (коды доступных движков), `search(query, *, search_engine=None, fetch_engine=None, …)` (движки опциональны, None → приватные `_default_search_engine()`/`_default_fetch_engine()`). Термин provider→engine **по всей цепочке**: настройки (ключи `search_provider`→`search_engine`, `content_provider`→`fetch_engine` + хелперы), код ошибки `search_provider_disabled`→`search_engine_disabled`. Тесты обновлены, dev-база вычищена от старых ключей, docs+memory. `--module=web_search` 49 / full **321 passed**. |
| [2026-07-05-tavily-drop-raw-content-at-search.md](2026-07-05-tavily-drop-raw-content-at-search.md) | 2026-07-05 | **Completed.** Убрана capability «тело при поиске» у Tavily (`include_raw_content` из `TavilySearchParams` шлюза) — стандартизация: поиск = только ссылки у всех провайдеров, тело тянет отдельная стадия `/extract`. Web_search-провайдер уже был двухстадийным (инлайн-контент снят ранее), поле в тонком коннекторе не использовалось (src 0, тестов 0). Приоритет стандартизации над полнотой коннектора — по решению пользователя. Класс-докстринг поясняет намеренный пропуск. Full **321 passed**. |
| [2026-07-05-web-search-settings-reorder-relabel.md](2026-07-05-web-search-settings-reorder-relabel.md) | 2026-07-05 | **Completed.** Порядок/лейблы настроек web_search на `/core/settings`: `default_pages` → «Страниц на поисковый запрос» сразу после сервиса контента; пара в конце — `max_concurrent_searches` «Максимум параллельных поисковых запросов» + `fetch_concurrency` «Максимум параллельных запросов получения контента». Реордер `SCHEMA`+хелперы+`__all__`; ключи/семантика не менялись → `searcher.py` не тронут; docs синхронизированы. `--module=web_search` 49 passed. |
| [2026-07-05-web-search-fold-save-into-searcher.md](2026-07-05-web-search-fold-save-into-searcher.md) | 2026-07-05 | **Completed.** `services/save.py` (одна `store_results`, вызывалась только сёрчером — фейковый «сервис») свёрнут приватным `_store_results` в `searcher.py` рядом с `_fetch_pages`; `save.py` удалён. Тест `test_save.py`→`test_store_results.py` (импорт на `searcher._store_results`). Довод: возврат `pending`-страниц = оркестрационный сигнал для `_fetch_pages`, не данные → дом оркестратора, не crud. docs+memory. `--module=web_search` 49 / full **321 passed**. |
| [2026-07-05-dev-schema-via-migrations-not-create-all.md](2026-07-05-dev-schema-via-migrations-not-create-all.md) | 2026-07-05 | **Completed.** Dev-схема теперь через **миграции**, а не `create_all`. Lifespan (`app_factory.py`) флипнут: `create_all` только для in-memory тестов (`sqlite_in_memory`), файловая dev-sqlite и PG — через Alembic. Предпосылка: **research_registry не имел миграций** → завёл `rrm_001..004` (+ `migrations_dir`). Пересобрал dev-базу (drop файла + `migrate upgrade`, 12 настроек/ключей сохранены). Паритет модель↔миграция на sqlite = 0 diffs; докстринги/`docs/platform/database.md`/MEMORY очищены от «create_all в dev». Full **321 passed**. |
| [2026-07-05-web-search-throttle-by-inflight-count.md](2026-07-05-web-search-throttle-by-inflight-count.md) | 2026-07-05 | **Completed.** Троттлинг параллельных поисков (несколько агентов бьют лимиты внешних API) в **режиме задержки, не ошибки**. Настройка `max_concurrent_searches`(3); гейт `_acquire_search_slot` перед `query_mark_processing` ждёт (sleep+jitter), пока свежих `processing` по движку `< limit` — **счётчик = строки в БД** (общий для процессов, per-provider). Reaper **inline** `query_expire_stale` (залипшие `processing` >15м → `error`; планировщика у модуля нет). Индекс `ix_web_search_query_engine_status` (модель+`wsm_001`). Фетч ограничен транзитивно; rate-гейт (RPM) отложен. CRUD `query_processing_count`/`query_expire_stale`, тесты (crud/searcher/settings), docs+memory. `--module=web_search` 49 / full **321 passed**. |
| [2026-07-05-module-description-field.md](2026-07-05-module-description-field.md) | 2026-07-05 | **Completed.** Добавлено `description: ClassVar[str]` в базовый `Module` (рядом с `name`) + русская проза на всех 6 модулях; прокинуто через settings-реестр `description()` → `_module_payload` → фронт `ModulePayload.description` → `VCardSubtitle` в карточке `/core/settings`. Только проза (title остаётся в хардкоде `MODULE_LABELS`); видно у settings-модулей (`core_gateway`/`web_search`). `tests/core/settings` 49 passed; dist не пересобран. |
| [2026-07-05-web-search-dto-into-models-core-dedup.md](2026-07-05-web-search-dto-into-models-core-dedup.md) | 2026-07-05 | **Completed.** web_search DTO перенесены из `dto.py` в модельные файлы рядом с ORM (`QueryRow`/`QueryDetail`→`models/query.py`, `PageRow`/`PageDetail`→`models/page.py`, `QueryResultView`→`models/query_result.py`); `dto.py` удалён. Общие в ядро: `Paged[T]`→новый `src/core/api/pagination.py` (экспорт из `core.api`); сериализатор даты `SqlDateTime` = дубль существующего ядрового `DatetimeUTCStr` (`core/utils/date.py`) → переиспользован. Заодно research_registry снят со своих дублей `Paged`/`SqlDateTime` на ядровые. docs+memory. Полный `pytest` 315 passed (research_registry без тестов — smoke-импорт). |
| [2026-07-05-web-search-drop-engine-base-duplicate.md](2026-07-05-web-search-drop-engine-base-duplicate.md) | 2026-07-05 | **Completed.** Убран общий предок `base.Engine` (наследование ради наследования) — `code`/`enabled_field`/`available()` продублированы в `SearchEngine`/`FetchEngine` (оба standalone `ABC`). Реестр: `bound=Engine` → constrained `TypeVar("E", SearchEngine, FetchEngine)`; `providers/__init__` без `Engine`; docs+memory. `--module=web_search` 43 / full 315 passed. |
| [2026-07-05-web-search-inline-upsert-helper.md](2026-07-05-web-search-inline-upsert-helper.md) | 2026-07-05 | **Completed.** Удалён `crud/_upsert.py` (`insert_for`); диалект-выбор INSERT инлайнен по месту в `page.py`/`query_result.py` (импорт `pg_insert`/`sqlite_insert` + две строки, поведение то же). Doc обновлён. `--module=web_search` 41 / full 313 passed. |
| [2026-07-05-web-search-rename-result-to-query-result.md](2026-07-05-web-search-rename-result-to-query-result.md) | 2026-07-05 | **Completed.** Сущность-связка переименована в «результаты запроса»: таблица `web_search_result`→`web_search_query_result`, модель `WebSearchResult`→`WebSearchQueryResult`, файлы `models/`+`crud/result.py`→`query_result.py`, DTO `ResultView`→`QueryResultView`, constraint/index, alias `result_crud`→`query_result_crud`. CRUD-функции (`result_add`/…) оставлены. Migration `wsm_003` обновлён (revision id сохранён). dev sqlite rebuild, тесты, фронт (api.ts/QueryView), docs+memory. `--module=web_search` 41 / full 313 passed; `migrate check` up to date; `vue-tsc` 0. |
| [2026-07-05-web-search-query-columns-rename-and-max-results-into-params.md](2026-07-05-web-search-query-columns-rename-and-max-results-into-params.md) | 2026-07-05 | **Completed.** Колонки `web_search_query`: `search_provider`→`search_engine`, `content_provider`→`fetch_engine`, `error_code`→`error`; убрана колонка `max_results` (теперь в `params` через `SearchRequest.to_params`). Follow-up: `web_search_page.error_code`→`error` тоже (симметрично). Настройки (`search_provider`/`content_provider`, хелперы `active_*`) не тронуты. Правки model/migration/request/crud/dto/api/searcher, dev sqlite rebuild, тесты, фронт (api.ts/store/QueriesView/QueryView/PageView/ru.json), docs+memory. `--module=web_search` 41 / full 313 passed; `migrate check` up to date; `vue-tsc` 0. |
| [2026-07-05-web-search-registry-classes-with-availability.md](2026-07-05-web-search-registry-classes-with-availability.md) | 2026-07-05 | **Completed.** Реестры движков web_search → **два класса** `SearchEngineRegistry`/`FetchEngineRegistry` (`EngineRegistry[E]` + синглтоны `search_engines`/`fetch_engines`), убраны dict+free-функции. **Учёт доступности через core_gateway**: общий предок `base.Engine` (`enabled_field`=gateway `ENABLED_FIELD`, `available()`=`service_enabled`), реестр даёт `available_codes()`/`is_available()`; searcher fail-fast при отключённом провайдере (`error_code=search_provider_disabled`). Правки base/registry/clients/провайдер-`__init__`/searcher, тесты, docs+memory. `--module=web_search` 41 / full 313 passed. |
| [2026-07-05-web-search-unify-statuses-four-per-role.md](2026-07-05-web-search-unify-statuses-four-per-role.md) | 2026-07-05 | **Completed.** web_search statuses → одна машина `pending → processing → done | error`, два набора по ролям: `SEARCH_STATUS_*` (query) + `FETCH_STATUS_*` (page). Убраны `empty` (пустая выдача = done) и `fetched` (= done); добавлен переход pending→processing (`query_mark_processing`, `pages_mark_processing`). Правки: constants/models/crud/searcher/save, миграции wsm_001/002 (CHECK+default), dev sqlite rebuild, тесты, фронт (api.ts/labels.ts/ru.json), docs+memory. `--module=web_search` 39 / full 311 passed; `migrate check` up to date; `vue-tsc` 0. |
| [2026-07-05-web-search-rename-contentfetcher-to-fetchengine.md](2026-07-05-web-search-rename-contentfetcher-to-fetchengine.md) | 2026-07-05 | **Completed.** web_search structure cleanup: content-role ABC `ContentFetcher` → `FetchEngine`; registry `register_content`/`get_content`/`content_codes` → `register_fetch`/`get_fetch`/`fetch_codes` (search-role unchanged); module file `module_settings.py` → `settings.py` (+ its test); deleted `normalize.py`, colocating helpers at point of use (`normalize_url`/`page_code`/`domain_of` → `crud/page.py`, `query_code` → `crud/query.py`). Settings `content_provider`/`active_content_provider` left as-is (names the setting, not the role). `--module=web_search` 40 / full 312 passed; docs+memory updated. |
| [2026-07-05-core-gateway-xai-service.md](2026-07-05-core-gateway-xai-service.md) | 2026-07-05 | **Completed.** New `core_gateway/services/xai/` gateway (tavily/firecrawl pattern): `XaiGateway` around Responses API (`responses()` = agentic web_search/x_search + citations URL-only; get/delete_response, tokenize, models/language_models/model, api_key_info) + typed `XaiResponsesParams`/tool models (`Xai*Tool` over `_XaiTool` extra=allow, tools via `SerializeAsAny`) + `constants` model-ids. Also added xai token settings. `--core` 266 passed; doc+memory updated. Media/voice/files/embeddings/legacy chat-completions out of scope; no live run (no key). |
| [2026-07-05-xai-search-research.md](2026-07-05-xai-search-research.md) | 2026-07-05 | **Completed.** Research xAI (Grok) content search → `AGENTS/research/xai/INDEX.md`: Agent Tools API on `/v1/responses` (web_search/x_search/code_execution/collections_search) + configs/limits, citations = URL-only (bucket C, needs A-rescrape), multi-agent deep research (`grok-4.20-multi-agent`, agent_count 4/16), models/pricing, legacy Live Search removed (410 Gone). Doc-based, no live calls. |
| [2026-07-04-research-queues-python-run.md](2026-07-04-research-queues-python-run.md) | 2026-07-04 | **Completed (demo/data).** Первое реальное исследование в `research_registry`: тема «Создание системы очередей на Python». Агент-оркестратор (services/ ещё нет): 3 подзапроса → live `search`+`research` (urb-research MCP) → запись в dev-БД через CRUD (seed `dev/bench/research_registry/seed_queues_research.py`): 1 research(done) → 3 queries(reported) → 15 docs(саммери+релевантность) → 3 отчёта. Видно в `/research/researches`. |
| [2026-07-04-design-system-code-block-restyle.md](2026-07-04-design-system-code-block-restyle.md) | 2026-07-04 | **Completed.** DS CodeBlock page brought under current styles: every section wrapped in `VCard.ds-card` + `h6.ds-card__title` (was floating `<h6>`/`.ds-section`), props table → nested `.ds-props` block, `.ds-page` flex column gap 20px. **+** fixed unreadable code on dark theme — `useHighlighter.ts` Shiki theme `github-light`→`github-dark` (app is dark-only); affects all CodeBlocks. `CodeBlockView.vue` + `useHighlighter.ts`; no i18n changes; dist not rebuilt. |
| [2026-07-04-core-mcp-port.md](2026-07-04-core-mcp-port.md) | 2026-07-04 | **Completed.** Port `core_mcp` 1:1 from the donor project: read-only «MCP-серверы» introspection page over `app.state.mcp_servers` (server list + tools + copy-paste connection config for Claude / Claude Code). Backend `src/modules/core_mcp/{__init__,module,api}.py` (guards dropped — no-auth fork), registered in `build_modules`; frontend `web/src/features/core_mcp/` + route + sidebar nav item (section «Мониторинг», `IconServerBolt`). Shared `McpInfoPanel.vue`/`mcp_tool_icons.ts` already present. vue-tsc clean; `web/dist` not rebuilt (no pnpm on PATH). |
| [2026-07-04-architecture-purpose-review.md](2026-07-04-architecture-purpose-review.md) | 2026-07-04 | **Completed (review-only).** Разбор архитектуры/предназначения: продукт = **research MCP-сервер**, веб = вспомогательный слой (настройка + просмотр/отладка). Разрыв: MCP-поверхности нет ни у одного модуля; auth-резолвер (`_collect_resolver`) блокирует монтаж в без-auth сборке; пайплайн ресёрча (план→чанк→эмбед→отчёт) не портирован из `../urb-mcp-research`. Решение: MCP-auth = статичный **токен в ENV-настройках ядра** (`core_setup`), ядро само ставит резолвер. Дом ресёрч-модуля пока НЕ зафиксирован (продолжение — `2026-06-27-research-pipeline-design`). |
| [2026-07-04-sidebar-monitoring-section.md](2026-07-04-sidebar-monitoring-section.md) | 2026-07-04 | **Completed.** Sidebar: new «Мониторинг» section above «Настройки», moved the tasks link into it (`AppSidebar.vue`); renamed `core_monitoring.nav` «Мониторинг задач»→«Задачи» (section already says «Мониторинг»). vue-tsc clean, dist rebuilt. |
| [2026-07-03-web-content-inline-fetch-redesign.md](2026-07-03-web-content-inline-fetch-redesign.md) | 2026-07-03 | **Completed.** Pipeline redesign per canvas: search status `fetching` (awaiting content; reclaim skips it), completion by terminal pages (fetched\|fail), provider `pages_per_request`, inline content when `max_results ≤ cap` (free via include_raw_content), batch fetch `fetch_concurrency × pages_per_request` (5×20). Live e2e verified. |
| [2026-07-03-web-content-search-retry.md](2026-07-03-web-content-search-retry.md) | 2026-07-03 | **Completed.** Mirror page retry mechanics onto search: `retry`/`fail` statuses + `error_count`/`error_code`/`error_at` columns + `search_max_attempts`/`search_retry_backoff` settings; provider error → `search_mark_error` (retry→fail). Dev tables hard-recreated. |
| [2026-07-03-web-content-retry-settings.md](2026-07-03-web-content-retry-settings.md) | 2026-07-03 | **Completed.** Add module settings `fetch_max_attempts` (3) + `fetch_retry_backoff` (300s), replacing the hardcoded constants; `crud/page` reads them via helpers. Auto-rendered on `/core/settings`. |
| [2026-07-03-web-content-max-results-column.md](2026-07-03-web-content-max-results-column.md) | 2026-07-03 | **Completed.** Move `max_results` (pages per query) out of `params` JSON into an explicit NOT NULL column in `web_content_search`, before `params`. Migration edited in place; dev tables hard-recreated. |
| [2026-07-03-web-content-provider-packages.md](2026-07-03-web-content-provider-packages.md) | 2026-07-03 | **Completed.** web_content providers → 3-file packages (client + handlers); queue-processing/parallelism moved from services into `ProviderHandler` (tasks delegate, no overlap). New `default_pages`=10 setting (max_results default), auto-rendered on frontend. |
| [2026-07-03-core-monitoring-tasks-ui.md](2026-07-03-core-monitoring-tasks-ui.md) | 2026-07-03 | **Completed.** Port `core_monitoring` from the donor project: read-only backend `/tasks` (list + 24h stats + runs + run logs) and frontend feature (TasksView + TaskRunsView). No-auth subset — guards/run-requests/monitor omitted. |
| [2026-07-03-web-content-two-task-pipeline.md](2026-07-03-web-content-two-task-pipeline.md) | 2026-07-03 | **Completed.** Rework web_content to the mail_sync pattern: `searcher.register_search` only queues; two scheduler tasks (`run_searches`, `fetch_pages`) execute with claim/reclaim + concurrency semaphore. `processing` status now wired. |
| [2026-07-02-web-content-processing-status.md](2026-07-02-web-content-processing-status.md) | 2026-07-02 | **Completed.** Add `processing` status to `web_content_search` + `web_content_page` (constants, CheckConstraint literals in wcm_001/002, frontend types/labels/i18n). Value not yet wired into transitions. |
| [2026-07-02-core-study.md](2026-07-02-core-study.md) | 2026-07-02 | **Completed.** Read-only walkthrough of `src/core` (~5300 lines, all subsystems) to build an up-to-date architectural picture; summary in chat, no code changed. |
| [2026-07-02-secret-field-manual-save.md](2026-07-02-secret-field-manual-save.md) | 2026-07-02 | **Completed.** Settings page: replaced per-field auto-save with a **single «Сохранить» button** (`SettingsView.vue` — editable `values` vs `saved` snapshot, `dirty` diff, `saveAll` PUTs changed keys then reloads; per-field errors). Secret field simplified to a plain password input (`SettingFieldSecret.vue`, saved by the one button; no more mid-typing reset). i18n `action.save`/`error.save_failed`. Backend unchanged. vue-tsc clean, dist rebuilt. |
| [2026-07-02-sidebar-settings-link.md](2026-07-02-sidebar-settings-link.md) | 2026-07-02 | **Completed.** Added a «Настройки» sidebar link → `/settings` (runtime settings page) in the «Настройки» section, above «Сервер» (`IconAdjustments`). `AppSidebar.vue` only; vue-tsc clean. |
| [2026-07-02-web-search-provider-in-core-setup.md](2026-07-02-web-search-provider-in-core-setup.md) | 2026-07-02 | **Completed.** Settings split: web_content provider **tokens** moved ENV→runtime DB settings (kept provider there too), core_setup now server/infra-only. Added a general **secret settings-field** capability — `StrField(secret=True)`, API masks on read (`""`+`is_set`), blank PUT = keep, password UI. web_content `tavily_api_key`/`firecrawl_api_key` secret fields + `provider_api_key()`; providers read from store; deleted `WebContentConfig`/`config_cls`; dropped «Веб-источники» from core_setup + `.env`. Tests +6, `--core --module=web_content` 300 passed, vue-tsc clean. NB: re-enter dev tokens on /core/settings; rebuild `web/dist`. |
| [2026-07-02-web-content-nav-rename.md](2026-07-02-web-content-nav-rename.md) | 2026-07-02 | **Completed.** Renamed the web_content sidebar section «Web Content» → «Веб-документы» (`web_content.nav` in `features/web_content/locales/ru.json` + hardcoded fallback `label` in `AppSidebar.vue`). Child items untouched. |
| [2026-07-01-scraper-module-extraction.md](2026-07-01-scraper-module-extraction.md) | 2026-07-01 | **Completed.** Assessed pulling `simple-scraper-api` (patchright browser scraper) into `src/modules`. Found the seam exists (`web_content` `SearchProvider.fetch_page`) but the stack is heavy (patchright+Chromium, exclusive persistent-profile lock, browser must live in worker) and reverses the lean-deps course. **Decision (user): skip — keep it a standalone daemon**, don't extract. Revisit form A (thin HTTP `fetch_page` provider to the daemon) if browser fetch is wanted later. No code changed. |
| [2026-07-01-web-content-viewer-ui.md](2026-07-01-web-content-viewer-ui.md) | 2026-07-01 | **Completed.** Read-only UI for `web_content`: new backend read-API on the module (`api.py`+`dto.py`, GET `/web-content/searches[/{id}]`+`/pages[/{code}]`, paginated list+filters via new `search_list`/`page_list`/`*_count`/`results_with_page_for_search`; `SqlDateTime` serializer to match the front Luxon `fromSQL` parser) and a Vue feature `web/src/features/web_content/` — two list→detail pages (Запросы / Страницы), nav group «Web Content», `MarkdownRenderer` for page content. Tests 26→37 (new `test_api.py` + crud list/count/join); `--core --module=web_content` 296 passed, `vue-tsc`+`vite build` ok. |
| [2026-07-01-web-content-tables.md](2026-07-01-web-content-tables.md) | 2026-07-01 | **Completed.** Renamed `web_ingestion`→`web_content` (module/classes/bench/docs/tests/memory) + built the core triple: models `web_content_search`→`web_content_search_result`→`web_content_page` (portable types; numeric `id` + hash `code` only on page = `text_hash(normalize_url)`, dedup by url; `unique(search_id,page_code)`), CRUD per entity + `_upsert` + `services/save.py::save_search_results`, `normalize.py` (url→https). Alembic `wcm_001..003` with **dialect-agnostic types** (`json_value`/`timestamp` → `migrate upgrade` runs on both SQLite and Postgres; core `com_*` converted too; PG parity verified). SQLite runtime still `create_all` at lifespan (flip to Alembic = separate decision). Providers `providers/` (base `SearchProvider.search()`+`fetch_page()`; `registry` + `tavily`/`firecrawl`). Pipeline: settings `ChoiceField provider`, `services/searcher.run_search`, `services/fetcher.fetch_due_pages`, scheduler task `web_content.fetch_pages` (cron, core task-lock). `--module=web_content` 20→26 passed; full pipeline verified live (search→pages→fetch). Deferred: external trigger (MCP/route), worker enable in dev, vector layer. |
| [2026-06-29-web-ingestion-module.md](2026-06-29-web-ingestion-module.md) | 2026-06-29 | **Completed.** New module `web_ingestion` (web search + content fetch + markdown standardization — "raw data module" of the research memory). `WebIngestionConfig` with `TAVILY_API_KEY`/`FIRECRAWL_API_KEY` (env-backed); registered in `build_modules`; tokens editable in `core_setup` (group «Веб-источники», secret) + written to `.env` (+ example placeholders); pure config test; docs hub + memory row. No DB/router yet. |
| [2026-06-29-tests-inmemory-db.md](2026-06-29-tests-inmemory-db.md) | 2026-06-29 | **Completed.** Tests moved off the Postgres 8-DB pool onto **in-memory SQLite** (`DB_PROVIDER=sqlite`+`DB_PATH=:memory:`, StaticPool). `TEST_DB_NAME/USER/PASSWORD` removed from env; `tests/conftest.py` rewritten (no pool, `--dbs` now no-op, no `_wipe_test_schema`, `-n auto`); 7 lifespan tests reclassified `heavy→db`, the 3 real Alembic tests stay `heavy` and skip unless `TEST_PG_DSN` (Postgres opt-in). `Config.sqlite_in_memory` added. `--all` 261 passed + 3 skipped. Docs/memory swept. NoSQL rejected — SQLAlchemy/Alembic stack; sqlite in-memory chosen. |
| [2026-06-27-research-prune.md](2026-06-27-research-prune.md) | 2026-06-27 | **Completed.** Cleared `AGENTS/research` to empty: deleted all 13 old-project research topic folders (intercom/gmail/fastmcp/auth/lmstudio/anthropic/country-data/donor-company/nginx-mcp/llm-scoring/html-sanitization/rate-limiting/vuetify), kept INDEX.md (inventory → empty) + TEMPLATE.md. |
| [2026-06-27-tasks-prune-deleted-modules.md](2026-06-27-tasks-prune-deleted-modules.md) | 2026-06-27 | **Completed.** Pruned the task log after the module strip: deleted **256** root tasks tied to removed modules (+ module-tied chat/message UI) and **emptied the archive** (61 files incl. its INDEX). Kept **65** files (core/platform, build/deploy, dev + agent infra, generic shell + design-system, strip tasks, new research-pipeline task). Rebuilt INDEX from kept rows (2 in-work + 53 completed), no dangling links. Not git-recoverable (no repo on this path) — done under explicit authorization. |
| [2026-06-24-remove-non-core-modules.md](2026-06-24-remove-non-core-modules.md) | 2026-06-24 | **Completed.** Deleted all 8 non-`core_` modules (phase 1) **+ `core_geo`** (ph3) **+ `core_security`** (ph4) **+ `core_monitoring`** (ph5) **+ the last 3 `core_mcp`/`core_storage`/`core_users` (ph6 → ZERO modules, bare `src/core`)** — backend dirs + tests, `build_modules` now `[]`; ph6 also patched core so it builds with no auth module (`INTERNAL`/`STORAGE_DEFAULT_GUARDS` + core settings guards → `allow_all`); verified `create_app([])` builds. frontend features (+ surgical `administration` keep-users/drop-contacts), router/sidebar/locales, doc hubs, benches, memory files, routing. core_security drop took `core_storage`'s `SvgValidator` with it (SVG now detected→refused, not sanitized). js + AGENTS docs swept of all deleted modules (sub-agents), examples re-pointed to `core_users`. Fixed pre-existing `tests/core/test_migrations.py` breakage. DB untouched. `--core` 235 + `--module=core_storage` 579 + heavy migration 3 passed. CLAUDE.md/AGENTS.md symlinks repointed to the fork. Pending: research/plans/education for deleted modules (archive vs delete). |
| [2026-06-17-ds-spoiler-hover-transitions.md](2026-06-17-ds-spoiler-hover-transitions.md) | 2026-06-17 | **Completed.** `Spoiler.vue`: (1) hover colour transitions — added `color 0.15s ease` to `.spoiler__head`/`.spoiler__chevron`/`.spoiler__title` (was snapping); (2) custom header colours — new props `color`/`activeColor` → CSS vars `--spoiler-color`/`--spoiler-active-color` with per-variant defaults as fallback; showcase "Custom colours" section + usage snippet + i18n `colors`/`colorTitle`/`dangerTitle` (en/ru). `vue-tsc` 0. **Incident:** a scripted locale edit (rule violation) forced a `git checkout` that discarded uncommitted `spoiler`+`message` i18n in both locale files; recovered by hand (EN from captured diff, RU `spoiler` from session capture, **RU `message` re-translated from EN — needs owner review**). JSON valid, EN/RU key parity. |
| [2026-06-17-ds-spoiler-component.md](2026-06-17-ds-spoiler-component.md) | 2026-06-17 | **Completed.** New reusable `web/src/components/Spoiler.vue` — clickable header (chevron+title) over `VExpandTransition`, `v-model:boolean` (default closed), `title`/`#title`/`#actions` slots, `disabled`; 3 styling themes via `variant`: `default` (bordered panel), `minimal` (borderless uppercase label), `card` (filled `surface-hi` header). Showcase page `/design-system/spoiler` (`structure/SpoilerView.vue`, group `structure`, `IconChevronDown`) + route + index card + `{en,ru}` locales + doc bump (37 pages). `vue-tsc` 0; verified live (3 themes, expand/collapse + chevron rotation, independent demo state). Shared basis for `2026-06-17-mail-sync-body-history-spoiler` (use `variant="minimal"`). |
| [2026-06-14-page-header-smart-back.md](2026-06-14-page-header-smart-back.md) | 2026-06-14 | **Done (vue-tsc clean; manual check pending).** `PageHeader` back arrow now returns to the actual previous in-app page instead of a fixed parent. New `composables/useNavigationHistory.ts` (`recordNavigation(from)` stores prev when `from.matched.length>0`, else null; `goBack(router, fallback)` → `router.back()` if prev exists else `router.push(fallback)`); wired in `router/guards.ts` `afterEach`. `PageHeader.vue` `backTo` is now the deep-link **fallback** (~50 call sites unchanged). Unified ad-hoc `window.history.length` goBack in `mail_sync/ThreadView` + conversations/intercom `ConversationView` onto the shared composable. |
| [2026-06-13-dev-docker-nginx.md](2026-06-13-dev-docker-nginx.md) | 2026-06-13 | **Built + edge-verified.** Dev nginx-фронт `dev/docker/` (single origin как в проде) для проверки приватной зоны (`X-Accel-Redirect`/`internal`), которую Vite не умеет. Один контейнер `nginx:1.27-alpine`, `network_mode: host`: `/`→Vite :12100 (HMR-ws), `/internal`,`/api`,`/webhook`,`/mcp`→backend :12200, `/storage/public/` alias, `/storage/protected/` `internal;`+alias. Smoke: `nginx -t` ok, public→200, protected напрямую→404. Оговорка: бэкенд-эндпоинт X-Accel ещё не реализован (отложен до `core_auth`, см. `2026-06-02-server-docker-migration`) — сквозной сценарий заработает с ним. |
| [2026-06-11-task-requests-open-task.md](2026-06-11-task-requests-open-task.md) | 2026-06-11 | **Done.** `/tasks/requests` → drill into the requested task's runs/logs detail. User decision: **open the detail to manager** (was admin-only), keep `/tasks` overview admin-only. Backend `core_monitoring/api/tasks.py`: `admin:read`→`manager:read` on the 3 detail endpoints (`GET /tasks/{m}/{c}`, `.../runs`, `.../runs/{id}/logs`). FE: route `/tasks/:module/:code` subject admin→manager; `TaskRequestsView` task cell → clickable `.task-link` (chevron on hover) `router.push`; `TaskRunsView` back-button computed by ability (`can('manage','admin') ? '/tasks' : '/tasks/requests'`). **Then (follow-up):** lowered the whole task section to the manager bar — `GET /tasks` overview + route `/tasks` + nav `coreMonitoringNav` `admin`→`manager`; now every task-related BE endpoint + FE route/nav is manager-gated (manager and above). `/monitor/locks/{key}` stays admin (lock monitoring, not tasks, no FE consumer). Reverted the ability-based back-button → plain `back-to="/tasks"`. Test contract `test_route_gating_contract`: `/tasks*` manager, `/monitor*` admin. 6 passed `--dbs 1-4`; vue-tsc clean. |
| [2026-06-08-ds-action-panel-prototype.md](2026-06-08-ds-action-panel-prototype.md) | 2026-06-08 | New DS page `/design-system/action-panel` (in the «Адаптация» group, next to breakpoints) — interactive **prototype** for marking a page's actions (Фильтры `panel` / Обновить, Создать `button`) and relocating the `bottom`-marked ones into a mobile **bottom action panel**; `panel` actions open a slide-up sheet. Self-contained `ActionPanelView.vue` (does NOT touch App.vue/layout store/real pages); mobile is **simulated** via a fixed-width frame + Desktop/Mobile toggle (no window resize needed). Computeds split inline/bottom/inline-panel by placement×`previewMobile`. Route + index card (`IconLayoutBottombarExpand`) + i18n (ru+en). Successor to the reverted `2026-06-06-mobile-filter-slot-experiment`, re-prototyped in DS first. vue-tsc clean; browser-verified both modes incl. sheet; no console errors. v1 — marking per-session, drag-grip visual-only. |
| [2026-06-06-mobile-filter-slot-experiment.md](2026-06-06-mobile-filter-slot-experiment.md) | 2026-06-06 | **REVERTED (2026-06-08).** The cross-page mobile FAB + bottom-sheet experiment (component registration via `useMobileActions`) was removed from the live conversations page per user request — premature for production; to be re-prototyped on the design-system page first. Deleted `useMobileActions.ts`; stripped `mobileActions*` from `layout/store.ts`; removed sheet+FAB+CSS from `App.vue` (and `IconFilter` import); `ConversationsView` renders `<ConversationsFilters>` unconditionally; dropped `activeFilterCount` (FAB badge) + i18n `common.action.actions`. `ConversationsFilters` kept as a plain responsive panel (6→3→1 col). `vue-tsc` clean; browser-verified @532/1068/1400px, no console errors. **Firm finding retained:** never `<Teleport>` out of a `KeepAlive`+`out-in` page (crashes `shouldUpdateComponent`/`emitsOptions`) — use component registration. Memory `mobile-action-sheet-pattern` + `responsive-testing-process` (KDE/X11 xdotool resize method). |
| [2026-06-06-interface-responsive-adaptation.md](2026-06-06-interface-responsive-adaptation.md) | 2026-06-06 | Adapt page interiors (follows nav work). (1) New DS group **«Адаптация»** after colours → `BreakpointsView.vue` (`/design-system/breakpoints`) — live `useDisplay()` indicator + six-bp reference scale with dashed `md`-threshold marker. (2) **conversation_insights reports** (debugged @360/600/960/1280): index grid `repeat(3)`→3/2/1 by bp; **`PieChart`** side-legend was clipped on narrow cards (even @960 md=4) → added `narrow` (width<height+160 via ResizeObserver) that stacks the legend as a centred row under the donut (shared fix). Volume report needed only that; countries maps scale fine, its data table h-scrolls @360 (left as-is per user). vue-tsc clean. Next: more sections; maybe non-scroll countries table. |
| [2026-06-05-memory-footprint-reduction.md](2026-06-05-memory-footprint-reduction.md) | 2026-06-05 | Reduce per-process RSS (~160–200 MB). Measured: build_modules +93 / create_app +29 / pymorphy dict +21. Web-only processes waste ~31 MB on eager anthropic/openai/mcp imports (`llm_providers/module.py`) + ~21 MB on `core_nlp` warmup. Unused deps dateparser/inscriptis/parsel. Levers: lazy LLM SDK, worker-only morphology, dep cleanup. Investigation done; impl pending go-ahead. |
| [2026-06-04-worker-split-config.md](2026-06-04-worker-split-config.md) | 2026-06-04 | Worker-split **Part 1**: renamed scheduler env/config — `SCHEDULER_ENABLED`→`BACKEND_SCHEDULER_ENABLED` (default **false**; dev `.env`=true embedded, prod web=false→worker), `SCHEDULER_TICK_SECONDS`/`MAX_CONCURRENT_RUNS`→`WORKER_TICK_SECONDS`/`WORKER_MAX_CONCURRENT_RUNS` (engine knobs, shared embedded+worker). Config fields `backend_scheduler_enabled`/`worker_tick_seconds`/`worker_max_concurrent_runs`; `scheduler.start` reads them; `conftest` env updated. Entry point `app_api.py`→`app_backend.py` (+`.run` run-dev/watch-dev, docstring). Env (dev/prod examples + real `.env`) + docs (`dev/docs/ENV.md`, scheduler README, core-architecture, memory). 169 core passed, 113 routes, grep-clean. Deferred: Part 2 (`app_worker.py`+shared runtime), Part 3 (module↔FastAPI decoupling — overlaps `2026-06-03-api-zones`). |
| [2026-06-03-api-zones.md](2026-06-03-api-zones.md) | 2026-06-03 | **API zones + route guards — реализовано** (chunks 1-11). Зона `internal` активна (`api`/`webhook` = болванки `router/api.py`/`webhook.py`); модули дают `router_internal`/`router_prefix`, `create_app` собирает зону через `router/internal.py`; метка `@guard("kind",*args)` + `GuardRegistry` (`Module.register_guards`) + валидация на сборке `validate_guard_rules`; встроенные `public`/`disabled`; мок `auth`/`ability` (→ core_users). Глобальный гейт `BACKEND_API_ENABLED` (off = scheduler-only, 0 маршрутов); публичный `/internal/health`; rename `/api`→`/internal`; Swagger выключен. CLI `app_backend.py --backend-api-enabled`. core_users-болванка + `AGENTS/docs/router.md`. Остаётся: реализация `core_users` + зон `api`/`webhook`. |
| [2026-06-14-commit-protocol-format.md](2026-06-14-commit-protocol-format.md) | 2026-06-14 | **Done.** Pinned the user's commit format: **one `git add <path>` per file** (never `-A`/`-a`), stage only the task's own changes, then a single one-line English `git commit`. New memory `feedback_commit_protocol.md` + MEMORY.md §1 pointer; **Commit protocol** rule added to `agent-primary.md` (=AGENTS.md/CLAUDE.md symlink); both maintenance playbooks updated ("one-line command"→per-file add); docs-maintenance KEEP-ALWAYS classifier gained a feedback-file exception so the new file survives upkeep. |
| [2026-06-11-frontend-keepalive-double-load.md](2026-06-11-frontend-keepalive-double-load.md) | 2026-06-11 | **Done.** `/mail-sync/mailboxes` fetched its list **twice** on open. Cause: all routed views sit under one global `<KeepAlive>` (`App.vue`) → first show fires BOTH `onMounted`+`onActivated` (re-entry only `onActivated`); views calling `load()` in both double-fetched. Fix (canonical, per `AdminsView`): data load **only in `onActivated`**, `onMounted` keeps one-time dict/providers warmup. Swept all views, fixed **23**: 13 had `onMounted` removed entirely (pure dup load; dropped unused import; `McpView`/`TaggingRulesView` cold-start `loading=false` moved into `onActivated`), 10 kept `onMounted` for one-time setup only (loadDicts/users.load/dicts.ensure; `ProcessedConversationsView` keeps `normalizeSort()` sync-prologue before the `onActivated` load). Left as-is: `ProfileView` (`reset` is local, no fetch), `UserDetailView` (own first-mount guard). `vue-tsc` clean. Memory: `frontend_keepalive_double_load`. |
| [2026-06-11-frontend-tasks-store.md](2026-06-11-frontend-tasks-store.md) | 2026-06-11 | **Done.** Global Pinia **tasks store** (`web/src/stores/tasks.ts` `useTasksStore`) + brief-status endpoint, groundwork for a run-task control on `/mail-sync/filters`. Backend: `core/crud/tasks.py::brief_status_all()` (one pass — window `row_number() over (partition module,code order started_at desc)` rank-1 = latest status + `max(finished_at) where success` = `last_completed_at`; pairs w/o runs absent = never-run) → `GET /internal/core-users/tasks-status` (`manager:read`, DTO `TaskBriefStatus{module,code,status:str\|None,last_completed_at}`, iterates `get_registry().all()` so every task appears) — in core_users (owns store+enqueue; monitoring is admin-only). FE: `api/tasks.ts` (`fetchTasksStatus`+`requestTaskRun`, `on403:'throw'`), store (`statuses` keyed `module:code`, per-task `requesting` flags, `requestRun`→`false` on 409 "already queued"/rethrow else/refresh on accept). DS page `data/TaskStatusView.vue` @ `/design-system/task-status` (VTable code·status-chip·last-completed·Run + VSnackbar + raw store dump). Tests +3→18 (`--dbs 1-4`), vue-tsc clean. Follow-up: wire into mail-sync/filters. |
| [2026-06-11-task-user-requests.md](2026-06-11-task-user-requests.md) | 2026-06-11 | **Done.** User-requested scheduler-task runs: table `core_task_requests` (status pending/running/success/error, priority, requested_by, task_id; partial-unique pending pair = no duplicate queueing) **owned end-to-end by core_users** (migration `cu06` in its chain — FK to core_users impossible from core). Consumer = permanently-running watcher task `core_users.watch_task_requests` (cron `* * * * *`, ~55s inner loop @5s) serving requests by **pure observation of core_tasks** (pending→running/final via "run of pair started ≥ requested_at" — also closes the request-just-before-cron double-run case; spawns `run_entry` only when the pair is idle); core runner/ticker untouched by requests. `manual_run`→`user_request` renamed stack-wide; enqueue API `POST /internal/core-users/task-requests/{m}/{c}` gated `manager:update` (404/403/409/202), old monitoring `/run` endpoint removed, FE repointed. Tests +11 (`test_task_requests.py`); 232 core + 294 module + 24 heavy green. Gotchas: core-chain migration can't `depends_on` a module chain; `--core` with `--dbs 1` = phantom failures (needs full pool). Dev DB migrated, no drift. |
| [2026-06-11-ds-table-vtable.md](2026-06-11-ds-table-vtable.md) | 2026-06-11 | **Done.** DS: renamed Tables→**Data table** (`data/TablesView.vue`→`DataTableView.vue`) + new sibling **Table** page (`data/TableView.vue`) demoing plain `VTable` with native thead/tbody. Router slugs `tables`→`data-table` + `table`; index `data` group + `IconTableRow`; i18n en/ru `data-table`+`table`. **Styling:** `main.scss` `.v-table:not(.v-data-table)` replicates the data-table header (uppercase/`--surface-hi`/36px/border) + soft row borders + `--hover` tint → a bare `VTable` matches `VDataTable` by default. Browser-verified pixel-identical; vue-tsc clean. Dev login gotcha: Chrome MCP `fill_form` doesn't sync Vuetify v-model → set via native value setter + input/change events; dev creds live in the password manager/`.env`. |
| [2026-06-10-core-module-state-store.md](2026-06-10-core-module-state-store.md) | 2026-06-10 | **Done.** Core **generic per-module state store** for arbitrary runtime data (import cursors etc.) — "куда модулю положить состояние задачи". Table `core_modules_state` (PK `(module, **code**)` — user chose `code`; `value` **JSONB**; `created_at`/`updated_at`); sibling of `core_modules_settings` but for **internal machine state** (no schema/registry/API/UI). `CoreModuleState` model + `crud/module_state.py` (dialect-aware upsert) + bound `ModuleStore`/`module_store(code)` accessor + `Module.store` property; migration `c0re00010005` (`down_revision=c0re00010004`). Doc `platform/module-state.md` + memory router. Tests: 9 db (CRUD/JSONB nested/composite-key isolation/accessor/`Module.store`) + heavy migration assertion, green; dev DB migrated, no drift. **No current consumer**: the gmail `history_id` cursor (parallel task) went to a per-mailbox **column** (`ms11`), the better home for per-mailbox state — this store stands as general infra for genuinely module-global state. |
| [2026-06-07-ds-field-size-density-axis.md](2026-06-07-ds-field-size-density-axis.md) | 2026-06-07 | **Done.** Made Vuetify `density` actually drive field height — it was flattened by a hardcoded `.v-field__input{min-height:36px}`, so all densities looked identical. `main.scss`: base block = `default` (36px, tallest per Vuetify semantics); added `.v-input--density-comfortable` (32px) / `.v-input--density-compact` (28px) steps **after** the `.v-field` block (equal specificity → source-order wins), shrinking min-height + vertical padding; filled/underlined keep reserved top padding (label) → only padding-bottom touched. **Global** for all `v-field` (select/text/number/date) — every existing `density="compact|comfortable"` field now genuinely shrinks. `/design-system/selects` "Density"→"Sizes" section (px specs + note); i18n `section.selects.sizes` (ru+en). Verified in browser: 36/32/28px. |
| [2026-06-07-modal-fields-default-density.md](2026-06-07-modal-fields-default-density.md) | 2026-06-07 | **Done.** Follow-up to the density-axis fix: walked every `VDialog` and dropped `density="compact"/"comfortable"` from input fields & dropdowns (`VTextField`/`VTextarea`/`VSelect`/`VCombobox`/`VNumberInput`/`ProviderModelSelect`) so they render full-size (default 36px) inside roomy modals; checkboxes/switches/btn-toggles/alerts/lists left compact. 9 dialogs: Model/AgentConfig/Mcp edit, TaggingRules, CreateToken, SetPassword, Users-add, team member+contacts, mail Filters. **Trap:** `ProviderModelSelect` `withDefaults` density=`compact`, so dropping the prop keeps it small → passed `density="default"` explicitly. Page filter bars (McpView/ModelsView) and input-less dialogs out of scope. Each verified in browser; `vue-tsc` clean. |
| [2026-06-07-ds-multiselect-chips-default.md](2026-06-07-ds-multiselect-chips-default.md) | 2026-06-07 | **Done.** Multi-select selection chips styled per DS + chips made the **global Vuetify default**. `vuetify.ts`: `chips:true, closableChips:true` on `VSelect`/`VAutocomplete`/`VCombobox`. Vuetify has no conditional default, so `main.scss` Chips block (a) kills `.v-chip__underlay`, (b) neutralizes `.v-select--single/.v-autocomplete--single/.v-combobox--single .v-chip` back to plain 13px text with `.v-chip__close{display:none}`, (c) styles `--multiple .v-chip` (`--surface-hi` bg, `--border`, muted close). `chips:true` also makes Vuetify **ignore the `#selection` slot** → added `:chips="false"` to the two compact "+N" filters in `ProcessedConversationsView` (is_commercial, tags). DS demo simplified to bare `multiple` + opt-out note. Verified in browser; pattern: opt out with `:chips="false"`. Unrelated: `/internal/conversation-insights/processed` 500 (backend, not this change). |
| [2026-06-06-mobile-navigation.md](2026-06-06-mobile-navigation.md) | 2026-06-06 | **Done.** Responsive sidebar. Vuetify `display.mobileBreakpoint='md'` (mobile <960, matches PageHeader 959px). Below it: `AppSidebar` drawer flips `permanent→temporary` overlay (`v-model=drawerOpen` over shared `layout.mobileOpen`), rail disabled (`collapsed = !mobile && layout.collapsed`), logo header swaps collapse-chevron→close-X, route-watcher closes overlay on nav; new mobile-only `VAppBar` in `App.vue` (hamburger→drawer, brand→/home, avatar-initial→/profile). Verified @333/@788px (overlay+top-bar) + desktop branch via stale-plugin tab; vue-tsc clean. Browser caveat: tiling WM caps viewport ~788px → couldn't drive a real ≥960 viewport (resize_window/resizeTo/F11 all no-op). |
| [2026-06-06-ds-select-search-dropdown.md](2026-06-06-ds-select-search-dropdown.md) | 2026-06-06 | **Done.** New reusable `web/src/components/VSelectSearch.vue` — drop-in `VSelect` wrapper with a search row pinned atop the open menu (`#prepend-item`: `VTextField`+`IconSearch`, `autofocus`, `@keydown.stop`), in-place `computed` filter (by `itemTitle`), `#no-data` empty state, `@update:menu` query reset; `inheritAttrs:false`+`$attrs` pass-through + slot forwarding; `defineModel`. DS Dropdowns page renders `<VSelectSearch>` and the `CodeBlock` shows the **usage** (import + `<VSelectSearch v-model :items .../>`), not the impl. Added `vue` to shiki langs; i18n key `section.selects.searchInDropdown`. Verified in browser + `vue-tsc` clean. |
| [2026-06-04-docs-refresh-headless.md](2026-06-04-docs-refresh-headless.md) | 2026-06-04 | Refresh README + `dev/docs/` after the PyInstaller-desktop → headless-server migration (`src/app.py` entry, nginx/Vite static, `git pull` deploy, Qt + MCP apps gone). README + DEVELOPMENT de-Qt'd; BUILD.md rewritten as a deploy guide (per `new_structure/README.md`; Docker/nginx artifacts TBD); ENV.md verified accurate vs `Config`. |
| [2026-06-07-tasks-plans-maintenance-playbook.md](2026-06-07-tasks-plans-maintenance-playbook.md) | 2026-06-07 | **Done.** Created `AGENTS/agent-tasks-maintenance.md` (sibling of `agent-docs-maintenance.md`) — re-runnable sub-agent playbook with three jobs: (1) archive `completed` tasks older than 14 days (age from frontmatter `date`); (2) archive `completed` plans whose every related task is completed-or-archived (no age gate); (3) harvest loose ends (Follow-up/Open/Deferred/TODO) into a persistent code-verified `AGENTS/LOST-AND-FOUND.md` (DONE→Resolved, OPEN kept; dedup by source+item). 5-phase pipeline, all shared tables + LOST-AND-FOUND = central write-targets, sub-agents read-only, history never rewritten. Rewrote the Archive note in both INDEX files (playbook does it; manual still works) + pointer in `agent-primary.md`. First run is separate. |
| [2026-06-07-docs-subdir-grouping.md](2026-06-07-docs-subdir-grouping.md) | 2026-06-07 | **Done.** Grouped all flat `AGENTS/docs` root docs into area subdirs (option B): `platform/` (11, `core-`/api/db prefixes dropped), `frontend/` (9, `frontend-` prefix dropped), `workflow/` (3), `twenty/INDEX.md`; docs/ root now = `INDEX.md` + subdirs only. Fixed 213 inbound links (3 sub-agents on disjoint files + central `MEMORY.md`/`docs/INDEX.md`/`agent-primary.md`), incl. depth-breakage on moved files' outbound links and 2 pre-existing danglers to prior-deleted memory files. Link validation green. |
| [2026-06-07-docs-maintenance-playbook.md](2026-06-07-docs-maintenance-playbook.md) | 2026-06-07 | **Done.** Created root playbook `AGENTS/agent-docs-maintenance.md` — captures the memory/docs cleanup operation as a re-runnable sub-agent fan-out: classification scheme, phased pipeline (inventory → code-actuality audit → consolidate → reclassify conventions → rebuild router → hubs → validate), orchestration rules (disjoint doc homes; `MEMORY.md`/`docs/INDEX.md` rebuilt centrally), ready-to-paste sub-agent prompts, validation checklist (broken links + orphans + dups + **code-actuality**), lessons. Linked from `agent-primary.md`. |
| [2026-06-07-agent-memory-restructure.md](2026-06-07-agent-memory-restructure.md) | 2026-06-07 | **Done (via sub-agents).** Restructured `AGENTS/memory` + `AGENTS/docs` for findability. Memory **87→26** files; `MEMORY.md` **148 ln/23 KB → 96 ln/9.7 KB**, rebuilt as a 3-part router (behavioral rules + module-axis routing table + atomic gotchas). 16 code-shape conventions → `docs/conventions/{backend,frontend,db-migrations}.md`; new `docs/{platform,mcp,mail_sync}/`, `docs/intercom/module.md`, `docs/frontend-rules.md`, `docs/design-system.md`; ~50 promoted/dup memory files folded into hubs+deleted. `docs/INDEX.md` + `agent-primary.md` + `README.md` updated. Validation green (links/orphans/dups). Absorbed `memory-docs-boundary`. Plan: `plans/2026-06-07-agent-memory-restructure.md`. |
| [2026-06-07-memory-docs-boundary.md](2026-06-07-memory-docs-boundary.md) | 2026-06-07 | **Done — absorbed into `agent-memory-restructure` (Phase 1).** Decision **Model B**: memory↔docs divided by *loading model* not content; single-home rule; promote-on-sections. Rule recorded in `agent-primary.md`/`MEMORY.md`/`docs/INDEX.md` headers; dedup/promotion completed by the restructure task. |
| [2026-06-07-memory-db-schema-pruning.md](2026-06-07-memory-db-schema-pruning.md) | 2026-06-07 | **Done.** Stop storing DB-schema column dumps in memory (verify in `models/` instead). Deleted `mail_sync_schema.md` (pure 3-table dump, email DB); trimmed embedded column blocks from `project_core_llm_module.md` («DB schema (7 tables)»→pointer), `conversations_module_layout.md` (dropped «Schema»+«CRUD-инвентарь», kept PK/soft-delete decisions), `core_geo_module.md` («Schema highlights»→no-lat/lon decision only). Fixed 2 dangling `[[mail_sync_schema]]` links. Rationale/why-decisions preserved. Open follow-up: MEMORY.md index-bloat (12 fat inline entries, batch-rebuild pattern across 4 notes). |
| [2026-06-06-settings-remove-language.md](2026-06-06-settings-remove-language.md) | 2026-06-06 | **Done — FE-only, vue-tsc clean.** Removed the language-switch `VCard` from `/settings` (`SettingsView.vue`) + its dead code (`LOCALES`/`setLocale`/`AppLocale`/`locale`); lang/region now lives only on the profile «Язык и регион» card. Hid Settings from the sidebar: emptied `navBottom`, dropped unused `IconSettings`, guarded the `#append` bottom `VList`+divider with `v-if="visibleNavBottom.length"` (no dangling divider; `ProfileNavCard` still pinned). Route `/settings` kept — reachable by URL, just unlinked. |
| [2026-06-06-frontend-datetime-prefs.md](2026-06-06-frontend-datetime-prefs.md) | 2026-06-06 | **Done — FE-only, vue-tsc clean, browser-verified.** Locale/timezone/date-format prefs (localStorage `app.timezone`/`app.dateFormat`, default «Авто») wired through the central `shared/utils/date.ts` (`_zoned` `.setZone(currentZone)`; `_datePart` reads picked format; `fmtDateUtc` left UTC) via new reactive `plugins/preferences.ts`. Pulled the prefs OUT of the sidebar into a new profile **«Язык и регион»** card (language `VSelect` → live ticking date/time hero → timezone `VSelectSearch` → date-format `VSelect`); full IANA tz list (`Intl.supportedValuesOf`, DST-correct — fixed `UTC±X` rejected), labels `time · zone`. Two-card profile turned adaptive (`.profile-grid` auto-fit, full-width blocks stack below). Sidebar/profile polish batch: avatar→person icon as rounded-square (both places), collapse-toggle square footprint + nav-style `surface-hi` hover (both states), collapsed-rail re-centred by dropping the scrollbar gutter (`app-sidebar--collapsed`), **collapse state persisted** to `app.sidebar_collapsed`. Open (user's call): tz/format label style vs mock; mirror prefs into Settings. |
| [2026-06-05-hashing-rename-22hex.md](2026-06-05-hashing-rename-22hex.md) | 2026-06-05 | Renamed hashing helpers `content_hash`→`text_hash`, `fingerprint_dict`→`dict_hash`; unified both on 22-char lowercase-hex (SHA-256[:22], 88-bit) to drop the `_`/`-` from base64url. Renamed all src call-sites; widened `*_version` columns 16→22 (conversations/intercom `parser_version`, conversation_insight_results `agent_version`/`tagger_version`) via migrations `c11`/`ci07`/`i06` (content_hash cols already 22; mail_sync unbounded). Rewrote `test_hashing` + fixed 7 length/import asserts. Verified: hashing 9 passed, migrate check chains, 775 module + 2 heavy green. Caveat: representation change → one-off mass re-process/re-tag. completed |
| [2026-06-05-claude-md-headless-refresh.md](2026-06-05-claude-md-headless-refresh.md) | 2026-06-05 | Reconciled `CLAUDE.md`/`AGENTS.md` (= `AGENTS/agent-primary.md`) against the headless-server refactor that `2026-06-04-docs-refresh-headless` left out of scope. Rewrote **About** (Qt/PyInstaller → headless `src/app.py` + nginx/Vite static + `git pull` deploy), **Structure** (`SPEC: ModuleSpec`→`Module` subclass; dropped `src/gui/`/`dist/`; added `src/app.py`/`static/`/`storage/`/`tools/`/`dev/docs/`; `bench/`→`dev/bench/`; `.run/`→`dev/.run/`; `runtime` +prod), **Inventory** (`APP_PROFILE`→`APP_ENV` verified vs conftest+config; prod = headless-from-source not `dist/release` binary), **Running and building** (whole section was `app_run.py`/`app_build.py`/`watch-gui`/Qt → real `dev/.run/` configs + `migrate`/`build-web`). Testing/start-of-work/working-rules verified accurate, untouched. Docs only. |
| [2026-06-04-multiroot-views-keepalive-blank.md](2026-06-04-multiroot-views-keepalive-blank.md) | 2026-06-04 | **completed** — `/twenty/opportunities` (+ other dialog-bearing views) rendered a **blank content zone on the 2nd in-app visit**. Cause: `App.vue` wraps content in `<Transition mode="out-in" appear> → <KeepAlive> → component`, and `<Transition>` can only animate a **single-root** component; the 3 Twenty views + `intercom/AdminsView` + `conversation_insights/TaggingRulesView` were **multi-root** (`<PageLayout>` + sibling `<VDialog>`s = fragment). 1st visit OK (`appear` mounts directly); revisit runs leave→enter over the cached fragment, handoff breaks → blank. Console: `Component inside <Transition> renders non-element root node that cannot be animated`. Fix: moved the `<VDialog>`s **inside** `<PageLayout>` (teleport to body anyway) → single root; minimal diff = relocated closing `</PageLayout>` past the dialogs. **Then swept the whole codebase** (user: "проверь остальные модули") — audited every `*.vue` for content after the root `</PageLayout>` and fixed 9 more: `administration/UsersView`, `agents/{Agents,Mcp}View`, `intercom/{Contacts,Conversations}View`, `mail_sync/{Filters,Messages}View`, `team/PeopleView`, `design-system/feedback/DialogsView`. **14 routed views total**; no multi-root routed views remain. FE template-only, no tests; `vue-tsc` clean; verified in-browser (Сделки→Компании→Сделки + Агенты→Члены команды render on revisit, warning gone). |
| [2026-06-04-design-system-defeature.md](2026-06-04-design-system-defeature.md) | 2026-06-04 | Dissolved the `design-system` FE feature (it's template chrome, not a domain module). `git mv` into root homes per the `auth` precedent: 26 views → `web/src/views/design-system/` (views use only `@/...` imports → untouched), `routes.ts` → `web/src/router/design-system.ts` (lazy imports `./views/X`→`@/views/design-system/X`), `locales/` → `web/src/locales/design-system/` wired explicitly in `plugins/i18n.ts` (namespace `design-system` no longer glob-derived but stable), `nav.ts` deleted → single link inlined in `AppSidebar` navBottom (like `/settings`, `IconPalette`). `router/index.ts` import `./design-system`. Deleted empty `features/design-system/`. No leftover refs; URLs + namespace unchanged; vue-tsc clean. |
| [2026-06-04-tasks-index-reconcile.md](2026-06-04-tasks-index-reconcile.md) | 2026-06-04 | Reconciled the tasks INDEX against each file's frontmatter `status`. Moved 42 finished rows (32 already `completed` + `route-loading-bar` + 9 whose work shipped but status was stale) from "In work" → top of "Completed"; flipped `status`→`completed` on those 9 (volume-report, twenty-i18n, core-geo-fixed-header-tables, tasks-ui-runs-detail, conversation-insights-{reports,frontend,module,agent}, conversations-module). Kept 8 genuinely-open rows in "In work" (worker-split Part2/3, api-zones api/webhook, usb-conversations-mcp-oversized-output, import-checkpoint, twenty-bench-entities, core-geo-business-data cg03/cg04, lmstudio, llm-providers-mcp-servers deferred bits). In work 50→8, Completed +42, no dups. |
| [2026-06-04-route-loading-bar.md](2026-06-04-route-loading-bar.md) | 2026-06-04 | **completed — vue-tsc clean.** Animated loading bar at the top of the content zone during slow route navigation (lazy-chunk import + `auth.ensureReady` `/me` await). New `router/progress.ts` (reactive `navigationLoading` + start/stop, `SHOW_DELAY=140ms` anti-flash for cached hops); `guards.ts` arms on `beforeEach`, clears on `afterEach`+`onError` (redirect re-arms → spans whole hop); `App.vue` renders indeterminate `VProgressLinear` (primary, h3) pinned `inset:0 0 auto 0` atop `VMain`, outside the route `<Transition>`, with its own `route-progress` fade. No tests (FE wiring). |
| [2026-05-31-design-system-bar-pie-charts.md](2026-05-31-design-system-bar-pie-charts.md) | 2026-05-31 | Design system: add bar (столбцы) + pie/donut (круговая) chart components & showcase pages, mirroring the D3-based `LineChart` pattern (palette, CSS tokens, ResizeObserver, hover tooltip, legend; route + index card + en/ru locales under the `charts` group) |
| [2026-05-31-sidebar-collapsed-divider.md](2026-05-31-sidebar-collapsed-divider.md) | 2026-05-31 | Sidebar: in collapsed (rail) mode the full-width `#prepend` `.sidebar-divider` sat midway between the centered collapse toggle and the first nav icon (no logo text/group label to anchor it), reading as a top border on the first item. Gated it with `v-if="!layout.collapsed"` — `#append` divider untouched; expanded keeps it (bottom still y=53, aligned with content header). Verified in-browser both states |
| [2026-05-31-tasks-dashboard-perf.md](2026-05-31-tasks-dashboard-perf.md) | 2026-05-31 | tasks dashboard perf: `GET /api/core/tasks` built 24h stats N+1 (one `session_scope`+`GROUP BY` per task, sequential `await`). New `crud.tasks.stats_24h_all()` = single `GROUP BY module,code,status WHERE started_at>=cutoff` → dict per pair; `_to_info` now sync + precomputed stats, `list_tasks` one query. `get_task` keeps single-pair `stats_24h`. No new index (`started_at` already indexed). +test `test_stats_24h_all_aggregates_per_pair`; 7 passed |
| [2026-05-31-tasks-ui-runs-detail.md](2026-05-31-tasks-ui-runs-detail.md) | 2026-05-31 | tasks UI: clickable cards → logs; task LIST persisted in localStorage (stale-while-revalidate, no card jitter); detail runs view shows skeleton on open (no stale data, no cache); detail filter panel → standard table layout + full backend sorting on runs (`sort_by`/`sort_dir` added to `/api/core/tasks/{m}/{c}/runs` + `crud.tasks.list_runs`) |
| [2026-06-03-agents-folder-direct-commit.md](2026-06-03-agents-folder-direct-commit.md) | 2026-06-03 | Agent layer commits directly: `AI-AGENTS/` snapshot + `sync-ai-agents.sh` retired, real `AGENTS/` folder committed (`.gitignore` ignores only root symlinks `AGENTS.md`/`CLAUDE.md`). Docs aligned. Supersedes `2026-05-31-agent-layer-git`. completed |
| [2026-06-01-python-version-downgrade.md](2026-06-01-python-version-downgrade.md) | 2026-06-01 | Lowered `requires-python` `>=3.14.4`→`>=3.12` + `.python-version`→`3.12`; re-synced on 3.12.3, lockfile updated, imports verified. completed |
| [2026-05-31-agent-layer-git.md](2026-05-31-agent-layer-git.md) | 2026-05-31 | Agent layer into git via `AI-AGENTS/` snapshot + `sync-ai-agents.sh`. **Superseded** by `2026-06-03-agents-folder-direct-commit` (direct commit) — historical. completed |
| [2026-05-31-memory-audit.md](2026-05-31-memory-audit.md) | 2026-05-31 | Audited `AGENTS/memory` against real code. Deleted obsolete `intercom_insights_module.md`; fixed stale facts in `project_modules_state` (3→9 modules), `run_configs` (filenames), `mail_sync_schema` (dropped admin/contact/builder_version cols), `i18n_frontend_setup` (fallback en, all features migrated), `design_system_state` (19→23 pages), `project_architecture` (module list in modules.py, on_startup hook), `project_core_llm_module` (8→7 tables), `project_agent_dispatch` (budget_tokens col dropped), `project_agents_feature` (chat tool-loop is implemented). Consolidation of 2 overlapping pairs left to user. |
| [2026-05-31-build-doc.md](2026-05-31-build-doc.md) | 2026-05-31 | Root `BUILD.md` for human contributors — build/release-only instructions distilled from `app_build.py`/`pyproject.toml`/`.env.example`/`README.md` (output layout, requirements, `uv sync --all-groups` + pnpm deps, build command + stages + flag table, prod-secrets `.env`/`cert/` deployed beside binary, run + icon notes). Naming: `BUILD.md` per root convention. README untouched |
| [2026-05-31-sidebar-scroll-zone.md](2026-05-31-sidebar-scroll-zone.md) | 2026-05-31 | Sidebar scroll confined to nav-items zone: moved logo header + divider from default slot into `#prepend` of `VNavigationDrawer`, so only `.v-navigation-drawer__content` (the nav `VList`) scrolls; logo top zone and `#append` bottom block stay fixed |
| [2026-05-31-branding-title-favicon.md](2026-05-31-branding-title-favicon.md) | 2026-05-31 | Branding: legacy title strings → "Uroboros.Research" in the 4 remaining title strings (index.html / FastAPI / Qt app name / .desktop); large `VBtnToggle` language switcher under the home hero; default locale `ru` → `en` (`i18n.ts`); favicon set in `web/public/` generated from `assets/app.ico` (icotool + ImageMagick, slimmed 16/32/48 ico) + linked in `index.html`; fixed `common.home.*` key-path bug. Verified in browser |
| [2026-05-31-home-landing-page.md](2026-05-31-home-landing-page.md) | 2026-05-31 | Standalone welcome page `views/HomeView.vue` at `/home` (hero + 4 capability cards + nav hint, copy derived from MCP "running a company through AI agents" framing); `/` redirect fixed from dead `/hh/vacancies` → `/home`; sidebar logo wrapped in a button → `router.push('/home')`; global `home.*` ru/en locales added. `vue-tsc` clean |
| [2026-05-31-settings-i18n.md](2026-05-31-settings-i18n.md) | 2026-05-31 | settings i18n (ru/en), **4th per-feature migration**. Reused the backend-owned-string pattern from tasks for `Field` metadata (`label`/`description`/option labels from `/api/core/settings/modules`): `useSettingText().localizeField(module, field)` derives `<module>.setting.<key>.<leaf>` → `settings.catalog.<module>.<key>.<leaf>` (catch-all for `llm_providers`, no frontend dir) → backend literal. Applied via a reactive `localizedModules` computed in `SettingsView` so the 8 leaf `SettingFieldXxx` components stay untouched and field labels switch **live** (verified). Chrome (`nav`/`page`/`action`/`module.no_fields`) + `catalog.llm_providers.*` (5 fields); sidebar entry got `labelKey:'settings.nav'`; `SettingFieldList` "Добавить" → new `common.action.add` (first entries in the shared `web/src/locales`). Endonyms + module names literal. `vue-tsc` clean |
| [2026-05-31-tasks-i18n.md](2026-05-31-tasks-i18n.md) | 2026-05-31 | tasks/Monitoring i18n (ru/en), **3rd per-feature migration**. Key architecture call: task `name`/`description` live on the bek (`NAME`/`DESCRIPTION` per class); frontend **derives** the i18n key from the stable `(module, code)` in the payload — **bek sends no key** (mirrors error-code policy) — with backend literal as final fallback. `useTaskText()` composable: `<module>.task.<code>` → `tasks.catalog.<module>.<code>` (catch-all for `core`/`llm_providers` which lack a frontend namespace) → literal. Per-module `task.*` dicts added to twenty/intercom/mail_sync/conversations/core_geo; `tasks` dict holds UI chrome + catch-all. Both views wired (`globalInjection:true` added for `$t` where `v-for="t"` shadows the i18n `t`). Status/level enums + cron kept literal. `vue-tsc` clean, ru↔en verified; fallback caught a live bek mail_sync task-split mid-task |
| [2026-05-31-design-system-i18n.md](2026-05-31-design-system-i18n.md) | 2026-05-31 | design-system showcase i18n (ru/en), **2nd per-feature migration after core_geo**: namespace `design-system` (hyphen resolves via `t()`); translated the index catalog (`index.group`/`index.page.<slug>.{label,tags[]}`, tags via `tm()`+`rt()`, component-name tags kept literal), each detail page's `PageHeader` title/description (`page.<slug>.*`), and categorical in-example section headings (`section.<slug>.*`, 20 pages; component-name prefixes baked into the value). All in-card demo content (button labels, `ds-spec`/`ds-label` captions, samples, RU comments) → **static English**, identifiers/props/CSS untouched. `tables`/`line-chart`/`world-map` = caption-only, no `section.*`. 23 views wired via parallel sub-agents over a centrally-authored locale. `vue-tsc` clean, verified ru↔en in-browser |

## Deferred

Tasks paused at the user's request. Move the row here from "In work" and set status to `deferred` in the task's frontmatter.

| File | Date | Description |
|------|------|-------------|

## Archive

Archiving runs via the [`agent-tasks-maintenance`](../agent-tasks-maintenance.md) playbook: a `completed` task older than 14 days (by frontmatter `date`) is moved into `AGENTS/tasks/archive/`, its row removed from this index and added to `AGENTS/tasks/archive/INDEX.md` (recreated on the next archival). The archive is currently empty.
