# Plans archive

Plans moved out of the main index at the user's request. One row per archived file.

| File | Date | Description |
|------|------|-------------|
| [2026-06-07-agent-memory-restructure.md](2026-06-07-agent-memory-restructure.md) | 2026-06-07 | **Done.** Restructured memory+docs around a module navigation axis. Memory 87→26 files; `MEMORY.md` rebuilt as a thin router (148→96 lines); code-shape conventions → `docs/conventions/`; new platform/mcp/mail_sync/intercom-module/frontend-rules/design-system docs; ~50 files folded into hubs. Validation green. |
| [2026-06-06-mcp-server-surface.md](2026-06-06-mcp-server-surface.md) | 2026-06-06 | **Done — core 220 + core_users 208 + team 6 + heavy 3 green.** Modules AS MCP servers under `/mcp/<code>`, each a separately-mounted standalone `fastmcp` fork (3.4.1, +13 MB backend-only, lazy) Streamable-HTTP sub-app. Declarative `Module.mcp_servers: dict[str,McpServerBuilder]` (key=`code`=URL segment, value=`mcp_server` ctor fn → lazy, no fastmcp in worker; dup code→RuntimeError) + injected `Module.mcp_token_resolver` (mirror of `guards`; core_users → `resolve_token`, keeps core module-agnostic). Core `src/core/mcp/` = sole fastmcp boundary (context/auth/audit/factory): agnostic `McpServerTokenVerifier.verify_token`→`resolve(token,"mcp")`→`AccessToken(client_id=str(id),scopes=[group])` (mount bypasses FastAPI `dependencies` so auth lives inside the server). `core/router/mcp.py::mount_mcp_servers` replaced `build_mcp_zone`/`MCP_DEFAULT_GUARDS`; lifespans composed in `app_factory` via `AsyncExitStack`. **Fork API corrected vs plan** (empirically verified): `http_app()` 1st-positional=`path` (transport=kwarg); ctor takes `version=`; **fork has NO `TransportSecuritySettings`** (its `ssrf.py`=outbound SSRF) → DNS-rebinding via ASGI `TrustedHostMiddleware`+new `Config.mcp_allowed_hosts`. team = 1st server (full CRUD members+contacts, mv'd from `temp/semaphore-old-mcp`, FastMCP under TYPE_CHECKING). Tests: verifier (pure), in-memory `Client` CRUD (db), mount/dup/missing-resolver/lifespan (app_factory); worker boot verified fastmcp-free. nginx = `2026-06-05-nginx-mcp-streaming`. |
| [2026-05-30-remote-database.md](2026-05-30-remote-database.md) | 2026-05-30 | Remote (self-hosted) Postgres connection hardening: env-driven strict verify-full TLS (`DB_SSL` + `DB_CERT`), connect/pool timeouts, explicit pool sizing; no behaviour change when `DB_SSL=false`. |
