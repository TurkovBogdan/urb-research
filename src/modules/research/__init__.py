"""research — модуль-оркестратор и реестр ресёрча.

Доменный модуль: владеет реестром ``Исследование → Запросы → Отчёт по запросу →
Документы`` и оркестрацией пайплайна. Потребляет ``web_search`` (поиск в вебе +
сохранение страниц) и ``core_connectors`` (внешние API), сам сеть не трогает. Выставляет
research-MCP-сервер как тонкий адаптер поверх того же CRUD, что и web-интерфейс.

Болванка: пока только зарегистрированный ``Module`` без таблиц/роутеров/MCP —
слои (models/crud/dto/services/api/mcp) добавляются послойно.
"""

from src.modules.research.module import ResearchModule

__all__ = ["ResearchModule"]
