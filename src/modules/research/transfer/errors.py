"""Отказ разбора архива — одна ошибка на все проверки формата.

Архив приходит извне, поэтому любой отказ обязан называть причину человеку: и порченый zip, и
чужой формат, и архив новее приложения ведут сюда. Ручки превращают её в 400 с этим же
текстом — придумывать код ошибки на каждую проверку незачем, действие у человека одно:
взять другой файл.
"""

from __future__ import annotations


class ArchiveError(Exception):
    """Архив не может быть разобран или применён; сообщение показывается пользователю."""


__all__ = ["ArchiveError"]
